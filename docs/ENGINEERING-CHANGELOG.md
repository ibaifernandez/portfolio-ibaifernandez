# Engineering Changelog

## Purpose

Single long-form technical history of the portfolio. This file consolidates the previous phase-by-phase notes into one maintainable source of truth.

## Current Architecture Snapshot

1. `src/pages/index.template.html`, `src/pages/cv-print.template.html`, and `src/pages/project-*.template.html` are generated by `scripts/build-pages.mjs`, which now also emits committed `.min` CSS/JS asset derivatives from readable source files.
2. Home sections are componentized and data-driven (`content/*.json` + `src/components/index/*`).
3. Projects now use:
   - a deterministic checkerboard layout (2 columns on desktop),
   - local secondary project pages generated from `src/pages/project.template.html`,
   - project navigation scaffolding (`back`, `prev`, `next`).
4. Quality gates are enforced by:
   - `npm run test:quality`
   - `npm run test:e2e`
5. **Hosting:** Netlify CDN (migrated from cPanel, 2026-03-02). Deploy triggered by push to `main` via GitHub Actions → `netlify-cli deploy`. Atomic deploys; zero-downtime.
6. **Contact form:** `netlify/functions/contact.js` — Node.js 20 Netlify Function using Resend API for email delivery. Migrated from `ajax.php` (PHP/PHPMailer). Current production contract: honeypot, timing check, input validation, and optional captcha verification.
7. **CI/CD:** `.github/workflows/ci.yml` runs build + quality gates + Playwright and deploys to Netlify only after CI green.
8. **SSL:** auto-provisioned by Netlify (Let's Encrypt). Renewed automatically. HTTP → HTTPS enforced at CDN level.

## Timeline Of Engineering Work

### 2026-03-27 - Homepage Visual Baselines Realigned After Editorial Iteration 2

- Audited the single failing visual regression after the second intentional homepage editorial pass:
  - `tests/e2e/visual.spec.js`
  - `experience, projects and logos visual baselines`
  - `tests/e2e/visual.spec.js-snapshots/projects-section.png`
  - `tests/e2e/visual.spec.js-snapshots/logos-section.png`
- Confirmed the failure was baseline drift, not a shared CSS/runtime regression:
  - `npm run build:pages` passed
  - `npm run test:quality` passed
  - repeated Playwright runs rendered stable `1160x2886` and `1160x396` sections against stale `1160x2887` and `1160x397` snapshots
- Refreshed only the homepage visual baselines that moved with the intentional March 27 source state and kept the rest of the visual surface untouched.
- Validation executed:
  - `npm run build:pages`
  - `npm run test:quality`
  - `npm run test:e2e`

### 2026-03-27 - Homepage Editorial Iteration 2 Tightened Hero, About, And Operating Model

- Applied a second bounded homepage editorial pass using the Brand DNA and homepage matrix as explicit review criteria.
- Tightened the hero without changing the front-door tagline:
  - kept `From narrative / to delivery.`
  - replaced the vaguer rotating descriptors with clearer outcome-oriented phrases around positioning, build, CRM automation, rollout, and post-launch delivery
- Reframed the about block away from CV-style accumulation and toward a clearer shared pattern:
  - `What holds the dossiers together`
  - `Different contexts, one operating pattern`
  - reduced the long eight-point inventory to four grouped bullets explaining the common operating logic across the portfolio
- Reframed the progress/capabilities section so it reads less like a generic skill wheel and more like an operating model:
  - new section framing explains these are linked delivery layers rather than isolated silos
  - several labels were renamed to sound more operational (`Front-End Delivery`, `Automation & Growth`, `Operations & Rollout`, `CRM & Revenue`)
- Added the matching EN/ES translation keys required for the new homepage copy.
- Validation executed:
  - `npm run build:pages`
  - `npm run test:quality`

### 2026-03-16 - Homepage Editorial First Pass Released As Shared Front Door

- Shipped the first bounded homepage editorial pass across the canonical home surfaces:
  - `src/pages/index.template.html`
  - `src/components/index/hero.html`
  - `src/components/index/services-section.html`
  - `src/components/index/experience-section.html`
  - homepage-facing datasets in `content/ctas.json`, `content/projects.json`, `content/services.json`, and `content/training.json`
  - aligned translation copy in `en.json` and `es.json`
- Reframed the homepage from a more theatrical, cryptic landing into a clearer editorial routing surface for the dossier system:
  - hero language now explains the narrative -> systems -> delivery connection more directly
  - several section headings were de-dramatized to improve clarity, hierarchy, and credibility
  - services and training copy were tightened to reduce overpromise
  - homepage-facing dossier cards were adjusted where the home copy needed a more accurate handoff into the underlying dossiers
- Fixed concrete homepage-facing drift already called out in thread guidance:
  - `Curent Location` -> `Current Location`
  - `downolad` -> `download`
  - legacy Elm St external link replaced with the internal dossier route
- Explicitly did not silently rewrite dossier canon from the homepage:
  - card-level framing was tightened only where needed for homepage routing
  - Norden positioning/public-status ambiguity remains a shared coordination concern rather than a homepage-owned decision
- Validation executed before release:
  - `npm run build:pages`
  - `npm run test:quality`
  - `npm run test:ci`
- Release implication for parallel threads:
  - treat the current homepage hero copy, section framing, card handoff, and shared homepage visual/test baselines as the new released state
  - do not keep working from the pre-edit homepage language or pre-refresh homepage assumptions when touching shared tests, snapshots, or home-facing translations

### 2026-03-16 - Post-Push CI Drift Pattern Documented For Shared Homepage Tests

- Documented the real failure mode behind the post-push `Quality · Build · E2E` incident so future threads do not recreate it:
  - CI evaluated committed `HEAD`, while local had extra uncommitted shared fixes
  - homepage translation assertions must follow `en.json` / `es.json`, not stale literals
  - homepage visual baselines must move with intentional canonical-source changes, not remain frozen out of habit
- Updated `docs/PARALLEL-SAFETY-BASELINE.md` with an explicit false-green warning and a homepage/test/snapshot coupling rule for parallel threads.
- Updated `docs/ENGINEERING-RUNBOOK.md` with a release protocol note that shared homepage source changes, shared E2E expectations, and visual baselines must travel together.
- Clarified that `npm run test:ci` includes `test:smoke`, which requires an environment able to bind `127.0.0.1:4173`; sandbox failures there are environmental, not automatically repository regressions.
- This change is documentation-only. No product runtime contract changed.

### 2026-03-16 - Release Contract Clarified Across Generated Outputs, Docs, And Thread Rules

- Updated `README.md` and `AGENTS.md` so the repo now states explicitly that committed `.min` CSS/JS files are generated by `npm run build:pages`, not hand-edited runtime sources.
- Added `docs/PARALLEL-SAFETY-BASELINE.md` as the shared mechanical contract for all parallel threads and linked it from the main repo guidance docs.
- Updated the thread prompts so master coordination, dossier threads, homepage editorial, and technical consolidation now all point explicitly to that shared baseline instead of depending on manual recap.
- Updated `docs/ARCHITECTURE.md` to make the generated-output contract more explicit:
  - root `*.html` pages plus committed `assets/css/*.min.css` and `assets/js/*.min.js` are now called out together as canonical generated derivatives
  - the `tests/e2e/` inventory now includes `norden-translation.spec.js`
  - the stale fixed `24 Playwright tests` wording was replaced with auditable suite wording tied to a dated verification (`28` tests listed on `2026-03-16`) and an explicit `npx playwright test --list` recheck path
- Updated `docs/ENGINEERING-RUNBOOK.md` with a safe push protocol for the multi-thread repo:
  - classify canonic sources separately from generated derivatives
  - rebuild before review when a source of truth changed
  - avoid packaging unrelated evidence/noise alongside shared release work
- Updated the shared E2E surface so intentional homepage changes stop reading as false regressions:
  - `tests/e2e/home.spec.js` now reads the hero pre-title expectation from `en.json` / `es.json` instead of a stale hardcoded English string
  - `tests/e2e/visual.spec.js-snapshots/projects-section.png` was refreshed after the intentional homepage editorial changes reduced the section height
- Updated thread-governance docs to match the actual CSS ownership state:
  - `docs/THREAD-ORCHESTRATION.md` and `docs/THREAD-PROMPTS/01-master-coordination.md` now point every thread at the same shared mechanical baseline instead of relying on oral handoff
  - `docs/THREAD-ORCHESTRATION.md` now records that LFi still has a shared `.lfi_dossier_*` residue in `assets/css/style.css` even though `assets/css/dossiers/lfi.css` exists
  - Norden guidance now reflects current public reality (`/norden.html`, `src/pages/project-norden.template.html`, `assets/css/dossiers/norden.css`) instead of describing the dossier as merely planned
  - `docs/THREAD-PROMPTS/04-lfi.md` now narrows the already-completed extraction claim to the real `lfi_newsprint_*` legacy block instead of implying the entire LFi shared block is gone
  - `docs/THREAD-PROMPTS/06-portfolio-ibaifernandez.md` and `docs/THREAD-PROMPTS/13-technical-consolidation.md` now treat Playwright counts as auditable state rather than timeless copy
- Validation:
  - `npm run build:pages`
  - `npm run test:quality`
  - `npm run test:e2e`

### 2026-03-16 - Technical Consolidation Thread Prompt Authored

- Added the dedicated technical-consolidation thread prompt:
  - `docs/THREAD-PROMPTS/13-technical-consolidation.md`
- Formalized technical-consolidation-specific thread guidance:
  - canonical shared surface across the whole public portfolio
  - source-of-truth shared technical files
  - allowed ownership of runtime, tests, build, deploy, discovery, and metadata contracts
  - explicit exclusion of dossier and homepage editorial rewriting
  - required turn-by-turn response format
- Resolved a real orchestration drift in shared-file inventory:
  - `llms.txt` now appears explicitly alongside `llms-full.txt`
  - `robots.txt`, `netlify.toml`, and `playwright.config.js` are now called out as shared reserved technical surfaces instead of being left implicit
- Documented the main technical fronts this thread must treat as live work:
  - Search Console and Bing verification remain partially manual and must not be claimed from repo state alone
  - CSP is still `Report-Only`
  - `docs/ARCHITECTURE.md` still hardcodes a `24 Playwright tests` claim that can drift from runtime reality
  - parity between `scripts/static-server.mjs` and `netlify/functions/contact.js` remains a shared contract, not an implementation accident
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect repository reality:
  - prompt `13` is now generated
  - technical-consolidation ownership boundaries are now explicit instead of implied through generic shared-file language
- This change is documentation-only. No product tests were executed.

### 2026-03-16 - Homepage Editorial Thread Prompt Authored

- Added the dedicated homepage editorial thread prompt:
  - `docs/THREAD-PROMPTS/12-homepage-editorial.md`
- Formalized homepage-specific thread guidance:
  - canonical homepage URL
  - source template and component surface
  - home-facing datasets and translation files
  - allowed file ownership
  - required turn-by-turn response format
- Resolved a real ownership ambiguity in the shared-thread model:
  - the homepage thread may now explicitly edit home-facing datasets and `en.json` / `es.json` when the change is strictly for homepage narrative, cards, CTAs, testimonials, services, training, or experience framing
  - broad shared CSS, JS, translation-runtime, build, and infrastructure work remains outside homepage ownership
- Documented the current public drift this thread must address:
  - typos such as `Curent Location` and `downolad`
  - overblown section labels such as `Engineering World Domination for 2 Decades`, `Praise Parade`, `Words that Warm the Heart`, and `The Faithful Few`
  - the legacy external brand link `https://elmst.net`
  - shared project-card positioning drift when homepage metadata no longer cleanly matches the current dossier direction
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect repository reality:
  - prompt `12` is now generated
  - homepage ownership boundaries are now explicit instead of being implied through dossier-only rules
- This change is documentation-only. No product tests were executed.

### 2026-03-16 - Norden Dossier Translation Hooks Completed

- Localized the Norden dossier source template in `src/pages/project-norden.template.html` with `translate`, `translate-html`, `translate-content`, and `translate-aria-label` hooks so page copy, metadata, and project navigation can switch cleanly between English and Spanish.
- Extended the shared Norden card payload in `content/projects.json` with matching translation hooks for the exhibit mockup so the homepage card remains language-aware alongside the dossier page.
- Added the full Norden keyset to `en.json` and `es.json`, and normalized the remaining default English fallbacks in the cohort segment labels to avoid mixed-language flashes before translations apply.
- Hardened the shared translation runtime in `assets/js/translate.js` so language JSON loads bypass stale local caches during development and after rebuilds, which keeps `?lang=es` reliable on generated dossier pages.
- Rebuilt generated pages and re-ran quality validation after the public translation layer was wired.

### 2026-03-16 - AGLAYA Thread Prompt Authored

- Added the dedicated AGLAYA dossier thread prompt:
  - `docs/THREAD-PROMPTS/10-aglaya.md`
- Formalized AGLAYA-specific thread guidance:
  - canonical public URL
  - current source template
  - expected dossier-local CSS path
  - allowed file ownership
  - required turn-by-turn response format
- Documented the key drift and evidence boundaries for this case:
  - the shared project card in `content/projects.json` still frames AGLAYA as `B2B Innovation Ecosystem`, while the dossier itself is now centered on a tighter conversion-first remote-agency story
  - the public dossier still exposes placeholder-style proof language through `Suggested proof asset` blocks
  - the education branch remains intentionally outside the center of this dossier and should continue to stay separated from the Route / 2x2MKT case
  - the local `aglaya.biz` WordPress workspace is now explicitly treated as read-only evidence for this thread, not as writable scope
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect the audited repository reality:
  - prompt `10` is now generated
  - AGLAYA is explicitly documented as a current audited exception where dossier selectors still live in `assets/css/style.css`
  - CSS extraction toward `assets/css/dossiers/aglaya.css` is now part of the documented thread contract
- This change is documentation-only. No product tests were executed.

### 2026-03-16 - Elm St Thread Prompt Authored

- Added the dedicated Elm St dossier thread prompt:
  - `docs/THREAD-PROMPTS/09-elm-st.md`
- Formalized Elm St-specific thread guidance:
  - canonical public URL
  - current source template
  - expected dossier-local CSS path
  - allowed file ownership
  - required turn-by-turn response format
- Documented the key drift and evidence boundaries for this case:
  - the public dossier still exposes fake embed shells labeled `Featured YouTube Embed`
  - the page still uses visible `Reference video ID` proof scaffolding instead of resolved public proof treatment
  - the current `2x2` embed copy risks narrative spillover into the separate Route / 2x2MKT dossier
  - the source repo contains a real technical hardening and Netlify story, but the Elm St portfolio page should still remain centered on the cinematic brand case
  - the external `elm-st-web` repo is now explicitly treated as read-only evidence for this thread, not as writable scope
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect the audited repository reality:
  - prompt `09` is now generated
  - Elm St is explicitly documented as a current audited exception where dossier selectors still live in `assets/css/style.css`
  - CSS extraction toward `assets/css/dossiers/elm-st.css` is now part of the documented thread contract
- This change is documentation-only. No product tests were executed.

### 2026-03-16 - The Research Engine Thread Prompt Authored

- Added the dedicated The Research Engine dossier thread prompt:
  - `docs/THREAD-PROMPTS/08-the-research-engine.md`
- Formalized The Research Engine-specific thread guidance:
  - canonical public URL
  - current source template
  - expected dossier-local CSS path
  - allowed file ownership
  - required turn-by-turn response format
- Documented the key drift and evidence boundaries for this case:
  - the current public page is intentionally sales-first, but still sits inside a portfolio credibility contract
  - the template still exposes placeholder-style proof surfaces such as `Proof Surface A2-A5`
  - the current public tier presentation does not cleanly align with supporting GTM docs around `Professional` vs `Complete` contents
  - the source history still carries `53` raw prompts audited, `50` core prompts, and `55` workflow/package language across different stages
  - the external product workspace is now explicitly treated as read-only evidence for this thread, not as writable scope
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect the audited repository reality:
  - prompt `08` is now generated
  - The Research Engine is explicitly documented as a current audited exception where route-specific selectors still live in `assets/css/style.css`
  - CSS extraction toward `assets/css/dossiers/the-research-engine.css` is now part of the documented thread contract
- This change is documentation-only. No product tests were executed.

### 2026-03-15 - MyBoard Thread Prompt Authored

- Added the dedicated MyBoard dossier thread prompt:
  - `docs/THREAD-PROMPTS/07-my-board.md`
- Formalized MyBoard-specific thread guidance:
  - canonical public URL
  - current source template
  - expected dossier-local CSS path
  - allowed file ownership
  - required turn-by-turn response format
- Documented the key drift and evidence boundaries for this case:
  - the current stage caption still uses placeholder-style wording (`reserved`)
  - the template hardcodes product claims such as `50+` cards, `4` API domains, and `0` data lock-in
  - the MyBoard source repo is now explicitly treated as read-only evidence for this thread, not as writable scope
  - the prompt now requires claim verification or future-proof reframing before tightening the public narrative
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect the audited repository reality:
  - prompt `07` is now generated
  - MyBoard is explicitly documented as a current audited exception where dossier selectors still live in `assets/css/style.css`
  - CSS extraction toward `assets/css/dossiers/my-board.css` is now part of the documented thread contract
- This change is documentation-only. No product tests were executed.

### 2026-03-15 - Portfolio Dossier Thread Prompt Authored

- Added the dedicated Portfolio Ibai Fernandez dossier thread prompt:
  - `docs/THREAD-PROMPTS/06-portfolio-ibaifernandez.md`
- Formalized portfolio-dossier-specific thread guidance:
  - canonical public URL
  - current source template
  - expected dossier-local CSS path
  - allowed file ownership
  - required turn-by-turn response format
- Documented the core drift and ownership risk for this case:
  - the template still hardcodes unstable technical metrics such as `29/29` and a `29-test` suite reference
  - `docs/ARCHITECTURE.md` still references `24 Playwright tests`
  - the prompt now requires explicit verification or future-proof reframing of unstable metrics before tightening the public narrative
  - the prompt also makes the boundary with the technical-consolidation thread explicit so the portfolio dossier does not become a back door into shared infra/config/test ownership
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect the audited repository reality:
  - prompt `06` is now generated
  - Portfolio Ibai Fernandez is explicitly documented as a current audited exception where dossier selectors still live in `assets/css/style.css`
  - CSS extraction toward `assets/css/dossiers/portfolio-ibaifernandez.css` is now part of the documented thread contract
- This change is documentation-only. No product tests were executed.

### 2026-03-15 - Route / 2x2MKT Thread Prompt Authored

- Added the dedicated Route to Digitalization / 2x2MKT dossier thread prompt:
  - `docs/THREAD-PROMPTS/05-ruta-digitalizacion-2x2mkt.md`
- Formalized Route / 2x2MKT-specific thread guidance:
  - canonical public URL
  - current source template
  - current page-local stylesheet vs dossier-convention target path
  - allowed file ownership
  - required turn-by-turn response format
- Resolved and documented the current naming / metrics drift:
  - older docs still carry legacy `project-national-tech-evangelism` references
  - the current source of truth is `src/pages/project-ruta-digitalizacion-2x2mkt.template.html`
  - current public materials also drift between `10,600+` and `11,000+`, so the prompt now requires explicit reconciliation before tightening the narrative
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect the audited repository reality:
  - prompt `05` is now generated
  - the dossier already has page-local CSS in `assets/css/national-route.css`
  - migration toward `assets/css/dossiers/ruta-de-la-digitalizacion-y-2x2-mkt.css` is now documented as convention-alignment work, not first-time isolation
- This change is documentation-only. No product tests were executed.

### 2026-03-15 - Norden Dossier Shifted To Orange Palette And Flexbox Layout

- Reworked the public Norden dossier implementation around the brand's primary orange (`#FF6800`) instead of inheriting the blue-led atmosphere used by LFi.
- Rebuilt the dossier-local Norden layout system in `assets/css/dossiers/norden.css` so page and module composition now rely on Flexbox instead of CSS Grid.
- Updated the Norden thread prompt (`docs/THREAD-PROMPTS/11-norden.md`) so this Flexbox constraint is documented as a dossier-specific implementation rule for future iterations.
- Rebuilt generated outputs and kept the change dossier-local except for the minimal shared `content/projects.json` color adjustment required to keep the Norden card/exhibit aligned with the new palette.

### 2026-03-15 - GymTracker Thread Prompt Authored

- Added the dedicated GymTracker dossier thread prompt:
  - `docs/THREAD-PROMPTS/03-gymtracker.md`
- Formalized GymTracker-specific thread guidance:
  - canonical URL
  - source template
  - expected dossier-local CSS path
  - allowed file ownership
  - required turn-by-turn response format
- Resolved a documentation drift in public-surface references:
  - older docs still mention `gymtracker-cover-2.*` as the active card/hero surface
  - the current public preview source of truth is `content/projects.json`
  - the cover assets remain valid historical/supporting media, but not the current public contract by themselves
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect the audited repository reality:
  - prompt `03` is now generated
  - GymTracker is explicitly documented as a current audited exception where dossier selectors still live in `assets/css/style.css`
  - CSS extraction toward `assets/css/dossiers/gymtracker.css` is now part of the documented thread contract
- This change is documentation-only. No product tests were executed.

### 2026-03-15 - DebTracker Thread Prompt Authored

- Added the dedicated DebTracker dossier thread prompt:
  - `docs/THREAD-PROMPTS/02-debtracker.md`
- Formalized DebTracker-specific thread guidance:
  - canonical URL
  - source template
  - expected dossier-local CSS path
  - allowed file ownership
  - required turn-by-turn response format
- Resolved a documentation drift in naming:
  - current live DebTracker context labels are `Corporate View` and `Spite Driven`
  - older references to `Dev Reality` are now treated as legacy wording unless coordination changes the template labels later
- Updated `docs/THREAD-ORCHESTRATION.md` to reflect the repository reality:
  - prompt `02` is now generated
  - DebTracker is explicitly documented as the current audited exception where dossier selectors still live in `assets/css/style.css`
  - CSS extraction toward `assets/css/dossiers/debtracker.css` is now part of the documented thread contract
- This change is documentation-only. No product tests were executed.

### 2026-03-15 - LFi / Norden Narrative Split Formalized

- Updated the dedicated LFi thread prompt:
  - `docs/THREAD-PROMPTS/04-lfi.md`
- Added the dedicated Norden thread prompt:
  - `docs/THREAD-PROMPTS/11-norden.md`
- Updated `docs/THREAD-ORCHESTRATION.md` to make the split explicit:
  - LFi owns Norden as a client proof case under LFi
  - Norden owns the standalone `Norden Intelligence System` framing
- This change is documentation-only and exists to keep future parallel dossier threads aligned instead of duplicating or confusing the Norden narrative across two different portfolio surfaces.

### 2026-03-15 - Parallel Thread Execution Model Documented

- Added a dedicated orchestration document for the final portfolio push:
  - `docs/THREAD-ORCHESTRATION.md`
- Defined the 13-thread work model:
  - 1 master coordination thread
  - 10 dossier threads
  - 1 homepage editorial thread
  - 1 technical consolidation thread
- Formalized simple anti-conflict rules:
  - dossier threads stay local to template/CSS/assets
  - shared files remain reserved for coordination / consolidation
- Established the dossier-local CSS naming convention:
  - `assets/css/dossiers/<slug>.css`
- Created the first reusable prompt:
  - `docs/THREAD-PROMPTS/01-master-coordination.md`
- Updated documentation indexes in `README.md` and `AGENTS.md` so humans and AI agents can discover the new execution model quickly.

### 2026-03-15 - LFi Thread Prompt Authored

- Added the dedicated LFi dossier thread prompt:
  - `docs/THREAD-PROMPTS/04-lfi.md`
- Captured the current operating brief for the future LFi-only thread:
  - ownership boundaries
  - non-SaaS visual guardrails
  - current known layout problems
  - required source materials
  - proof areas already in scope
- This change is documentation-only and is intended to move future LFi work into its own isolated thread without losing the design and narrative lessons already learned.

### 2026-03-15 - LFi Layout Guardrails Pass (Hero Pairing + Proof Width + Lane Protection)

- Reworked the LFi hero into a three-lane composition so the thesis title owns the right edge while the explanatory lead stays visibly adjacent in a dedicated center column.

**Commit:** `working tree (uncommitted)`

#### Necessity

The LFi dossier was already in the right visual direction, but several layout edges were still breaking the reading experience: the hero support copy could still drift too low relative to the thesis, proof modules were not consistently occupying the full width made available to them, the timeline title still risked colliding with the floating right-side lane, and the client wall needed stronger logo and portrait presence to read as intentional proof rather than decorative marks.

#### Process

1. Locked the hero pair more explicitly:
   - top-aligned the hero grid
   - top-aligned the supporting copy block so it stays beside the thesis instead of collapsing downward
2. Protected the right-side editorial lane:
   - increased right padding on oversized right-column section heads
   - reduced the promotion-arc title scale so it stops fighting the viewport edge and floating controls
3. Flattened proof-body width where the title should not steal horizontal real estate:
   - POCURO and Leben titles now stay in row one only
   - proof bodies retain the full row beneath them
4. Increased client-proof visibility:
   - enlarged POCURO / Leben client logos inside snapshot cards
   - widened client-wall logo columns
   - enlarged testimonial avatars
5. Kept the page-local dark footer and centered project navigation as part of the canonical LFi styling contract.

#### Actual Results

- The hero now behaves more like a paired editorial composition instead of a large title with dropped supporting text.
- POCURO and Leben proof modules are free to use the full width of their own content row.
- Timeline and right-column oversized titles have a safer buffer before the back-to-top corridor.
- Client logos and testimonial portraits read more clearly as proof assets, not tiny decorative tokens.

### 2026-03-15 - LFi Composition Pass (Hero Lane + Client Snapshot + Timeline Repair)

**Commit:** `working tree (uncommitted)`

#### Necessity

The dark LFi dossier was finally in the right visual family, but several high-impact composition problems remained: the top chrome still collided in the hero, the oversized headings had no protected right-side lane, the client snapshot cards were underusing their own width, and the promotion arc was still reading like a cramped multi-column block instead of a progression.

#### Process

1. Tightened the top dossier chrome:
   - added more separation between the backlink row and the protocol line
   - aligned the top row more deliberately on a shared baseline
   - pushed the hero headline further into the right-side lane so the zig-zag rhythm starts immediately
2. Increased the editorial right margin and gave the floating back-to-top button its own safer corridor on LFi.
3. Rebuilt the `Systems architecture` section so the `01–04` operating modules now sit directly under the explanatory copy and signal list rather than drifting into a detached lower block.
4. Reworked client snapshot cards for POCURO and Leben:
   - split each card into text + brand areas
   - enlarged the logos
   - used the internal empty space of the card instead of stacking everything vertically
5. Repaired the promotion arc into a readable vertical sequence with room for copy, instead of preserving a cramped pseudo-column layout.

#### Actual Results

- The hero no longer needs to fight the top chrome for legibility.
- The hero supporting copy now stays beside the thesis block instead of collapsing toward the lower edge of the band.
- The LFi hero now pins both the thesis and the supporting copy to the same grid row so the opening reads as a paired composition instead of a headline with dropped copy.
- Oversized right-side headings have more breathing room before the viewport edge.
- POCURO and Leben intros read more like designed proof cards and less like stacked placeholders.
- The LFi role progression is now structured as a legible editorial timeline instead of three narrow, hard-to-scan text strips.

### 2026-03-15 - LFi Grid Placement Stabilized (Hero Chrome + Systems / Proof Realignment)

**Commit:** `working tree (uncommitted)`

#### Necessity

The dark LFi direction was finally the right one, but the actual grid placement was still unstable. The opening chrome could overlap visually, the language toggle needed more safe margin on the left, and the systems / proof sections were still allowing CSS auto-placement to create dead space and awkward stacking. In practice, that meant the page looked intentional in concept but fragile in execution.

#### Process

1. Increased left editorial padding so the floating language switcher has real breathing room instead of sitting on top of the dossier copy.
2. Tightened and stabilized the hero chrome:
   - reduced the headline scale slightly
   - reduced the hero top gap
   - aligned the top dossier labels more deliberately
   - removed the micro-overlap between backlink/protocol/title zones
3. Replaced section auto-placement with explicit row/column behavior:
   - systems: intro and signal list on the right, module grid directly below them
   - right-side proof sections: intro on the left, case proof directly below it, headline locked to the opposing side
   - left-side proof sections: mirrored structure for real zig-zag rhythm
4. Expanded proof identity in the client intro blocks:
   - POCURO logo retained and contextualized more clearly
   - Leben logo kept visible with explicit brand framing (`ileben.cl`)
5. Softened section handoffs further by extending the gradient fade at the bottom of each band.

#### Actual Results

- The LFi hero no longer feels like a broken hybrid between oversized title and dossier chrome.
- The `01–04` operating modules now belong to the systems explanation instead of drifting into a generic lower band.
- POCURO and Leben read more clearly as client proof modules, not floating labels.
- The left margin now gives the language switcher a safer landing zone on project pages.

### 2026-03-15 - LFi Structural Rhythm Pass (Header Alignment + Side Rails + POCURO Context)

**Commit:** `working tree (uncommitted)`

#### Necessity

The canonical LFi dossier had moved in the right visual direction, but the reading rhythm was still too flat in practice. The opening chrome felt misaligned, the top spacing still looked ceremonial, and the POCURO proof module entered with too little client context. The page also needed more lateral energy so the reader would not experience it as "big title, then a wall of text, then another big title".

#### Process

1. Reworked the top dossier chrome:
   - aligned the backlink row and public-dossier meta line more deliberately
   - tightened the space between the top chrome and the hero thesis
   - kept the project-page language toggle pinned further into the left margin so it does not collide with editorial copy
2. Strengthened section rhythm and zig-zag composition:
   - widened the left/right asymmetry of section grids
   - introduced subtle side-rail treatments instead of boxed cards
   - moved the operating-layer module list directly under the systems explanation so the section uses its available space more intelligently
3. Added more visual emphasis without turning the dossier into a SaaS dashboard:
   - new accent emphasis inside key oversized headlines
   - proof-side fact rail for POCURO
   - stronger lateral separation between thesis, explanation, and proof blocks
4. Reframed POCURO entry context before the numeric proof:
   - explicit client snapshot
   - initial-state framing before the compression funnel
   - operational fact strip to make the module legible before the reader reaches the chart
5. Trimmed redundant LFi CSS left over from previous iterations so the page could keep the stronger composition and still clear the repo budget gates.

#### Actual Results

- The LFi opening now reads faster and feels less visually unbalanced.
- The dossier alternates weight left/right with more intention instead of stacking oversized headlines in a monotone cadence.
- POCURO now enters as a client story with context, not as an unexplained acronym plus numbers.
- Validation after the pass:
  - `npm run build:pages`
  - `npm run test:quality`
  - `npm run test:e2e` (`26 passed`)

### 2026-03-15 - LFi Hero Sequencing Tightened Around Narrative Flow

**Commit:** `working tree (uncommitted)`

#### Necessity

The canonical LFi dossier had already improved the visual direction, but the opening sequence was still trying to prove too much too early. The hero mixed thesis, case-specific metrics, and shortcut CTAs before the reader had enough context to care about POCURO or Leben. That weakened the narrative authority of the opening instead of sharpening it.

#### Process

1. Reduced the empty top space before the LFi headline so the dossier enters its main idea faster.
2. Increased the hero lead to a stronger reading size (`2rem` on desktop) so the opening claim carries more weight beside the headline.
3. Removed the hero metric strip entirely:
   - no early case proof
   - no awkward arrow notation
   - no premature reference to POCURO or Leben before their modules appear
4. Removed the opening CTAs that were interrupting the dossier flow:
   - deleted the internal jump to proof modules
   - deleted the external `Visit LFi` CTA
5. Removed the closing `Visit LFi` CTA as well so the case study remains focused on Ibai's value, not on redirecting attention to the employer site.
6. Tightened project-page chrome around readability:
   - reduced the gap between the LFi backlink row and the opening protocol line
   - introduced a shared `project_dossier_page` body class across dossier templates
   - moved the floating language switcher to `left: 20px` on project pages while leaving the home offset unchanged

#### Actual Results

- The LFi dossier now opens as a cleaner argument: thesis first, proof later.
- The hero feels more deliberate and less like a dashboard header.
- The page keeps the user inside the narrative instead of asking them to skip or leave.
- The floating language button now sits in the left margin on dossier pages instead of colliding with editorial content.

### 2026-03-15 - LFi Canonicalized, Legacy Archived, And Home Project Covers Shifted To Typographic Mockups

**Commit:** `working tree (uncommitted)`

#### Necessity

The parallel LFi proposal had already surpassed the old public dossier in both authority and visual direction. Keeping the stronger version parked at `lfi-v2.html` while the weaker one remained canonical made no product sense. At the same time, the home grid still mixed premium project writing with more conventional image-first surfaces. The next release needed LFi promoted to the main route, the former dossier archived outside discovery, and the project grid pushed toward a more authored, text-led portfolio language.

#### Process

1. Promoted the former parallel proposal into the canonical LFi source:
   - `src/pages/lfi.template.html` now powers `lfi.html`
2. Preserved the former dossier as an internal archive:
   - moved to `src/pages/lfi-legacy.template.html`
   - generated as `lfi-legacy.html`
   - blocked from indexing via `meta robots="noindex,nofollow"` and `robots.txt`
   - excluded from sitemap and LLM discovery outputs
3. Added a compatibility redirect:
   - `lfi-v2.html` -> `lfi.html`
   mirrored in both `netlify.toml` and `scripts/static-server.mjs`
4. Strengthened the canonical LFi dossier with current proof already available:
   - POCURO qualification-compression module
   - Leben deliverability / hygiene / engagement recovery module
   - more explicit operational details (WhatsApp automation, lead scoring, blacklist recovery, SPF/DKIM/DMARC, SQL cleanup, Mailchimp -> Brevo)
5. Kept LFi visual proof native to the page instead of introducing a charting dependency:
   - custom HTML/CSS data visuals preserve editorial control
   - avoids generic SaaS-dashboard aesthetics
   - keeps asset weight and rendering complexity low
6. Converted the remaining home project cards to typographic mockup covers so the grid now reads like a curated portfolio system instead of a mix of screenshots and marketing tiles.

#### Actual Results

- `lfi.html` is now the real LFi dossier, not a placeholder parallel route.
- `lfi-legacy.html` survives only as a private reference surface outside normal discovery.
- Home project covers now use a consistent authored mockup language across the full grid.
- LFi communicates systems credibility through data visuals and operational proof, not through generic stock imagery.

---

### 2026-03-15 - LFi Dossier Reframe (Section Bands + Gradient Narrative Recovery)

**Commit:** `working tree (uncommitted)`

#### Necessity

The previous LFi visual iteration had broken down into an awkward hybrid: oversized editorial typography from one direction, legacy atlas spacing from another, and a flat beige page surface that made the whole dossier feel heavy, monotone, and visually unresolved. The user explicitly rejected that state and asked for a more modern section-by-section journey with color identity, smoother transitions, and less text-wall fatigue.

#### Process

1. Stopped trying to keep patching the inherited LFi atlas rules inside the global stylesheet and introduced a page-local override sheet:
   - `assets/css/lfi-atlas-override.css`
2. Updated `src/pages/lfi.template.html` to load the override after `style.min.css` and switched the page `theme-color` to a dark value aligned with the new art direction.
3. Reframed the dossier as a sequence of narrative bands rather than one continuous paper-colored surface:
   - hero
   - systems architecture
   - POCURO proof module
   - Leben proof module
   - promotion arc
   - enterprise proof
   - final close
4. Gave each band its own color identity and soft gradient transition so the user travels through the case instead of scanning one long beige sheet.
5. Moved proof and systems material into more deliberate layouts:
   - systems narrative + module grid
   - POCURO client context before metrics
   - data visuals embedded inside the proof modules
   - logo/testimonial wall grouped under enterprise pressure instead of mixed into earlier narrative
6. Tightened the hero chrome so the dossier enters faster:
   - less space above the protocol line
   - smaller gap between dossier labels and the title field
   - left-margin translator preserved for project pages only

#### Actual Results

- LFi is no longer depending on the old beige atlas styling to render correctly.
- The dossier now has a recoverable modern direction: darker, more segmented, more cinematic, and easier to refine section by section.
- Future polishing can focus on editorial rhythm and proof density instead of fighting broken inherited spacing.

---

### 2026-03-13 - Public Project Slugs Normalized (No More `project-` Prefix)

**Commit:** `working tree (uncommitted)`

#### Necessity

The public dossier URLs still carried an implementation-flavored `project-` prefix that added noise without adding clarity. One slug (`project-enterprise-crm.html`) was also actively misleading because the public case study had already evolved into the LFi dossier. The portfolio needed cleaner, more memorable public routes before the editorial polish pass and before external indexing work.

#### Process

1. Renamed the public dossier outputs in `content/projects.json`:
   - `project-debtracker.html` → `debtracker.html`
   - `project-gymtracker.html` → `gymtracker.html`
   - `project-enterprise-crm.html` → `lfi.html`
   - `project-ruta-digitalizacion-2x2mkt.html` → `ruta-de-la-digitalizacion-y-2x2-mkt.html`
   - `project-portfolio-ibaifernandez.html` → `portfolio-ibaifernandez.html`
   - `project-myboard.html` → `my-board.html`
   - `project-the-research-engine.html` → `the-research-engine.html`
   - `project-elm-st.html` → `elm-st.html`
   - `project-aglaya.html` → `aglaya.html`
2. Updated the build pipeline (`scripts/build-pages.mjs`) so canonicals, OG URLs, schema URLs, and per-project share image logic now resolve against the normalized dossier outputs.
3. Added permanent legacy redirects in `netlify.toml` from every old `project-*.html` path to its normalized canonical route.
4. Mirrored the same redirect map in `scripts/static-server.mjs` so local E2E behaves exactly like production.
5. Updated ancillary tooling and machine-readable outputs:
   - `scripts/generate-print-pdf.mjs`
   - `sitemap.xml`
   - `llms.txt`
   - `llms-full.txt`
   - QA route inventories in `docs/QA-DESKTOP.md` and `docs/QA-MOBILE.md`
6. Removed the obsolete generated `project-*.html` files from the repository so the root now contains only current canonical dossier outputs plus the legacy redirect behavior at runtime.

#### Actual Results

- Public dossier URLs are shorter, cleaner, and semantically aligned with the current product.
- Legacy traffic still has a safe migration path because all former `project-*.html` routes now redirect to their canonical successors.
- The repository no longer mixes live outputs with dead generated dossier variants, which reduces editorial and SEO confusion during future audits.

---

### 2026-03-13 - LFi Parallel Dossier Proposal Added For Side-By-Side Review

**Commit:** `working tree (uncommitted)`

#### Necessity

The next editorial/design pass needs side-by-side comparison, not blind replacement. LFi is the strongest candidate for a parallel proposal because the current dossier already has useful content, but still wants a more premium public frame that sells systems thinking rather than "agency work" in generic terms.

#### Process

1. Added a new non-canonical draft route:
   - template: `src/pages/lfi-v2.template.html`
   - generated output: `lfi-v2.html`
2. Kept the live dossier untouched at `lfi.html` so the proposal can be reviewed against the current public version without replacing it.
3. Designed the proposal with a deliberately different art direction from Research Engine:
   - mineral-paper background instead of black void
   - oversized editorial typography
   - border-led evidence rails instead of boxed SaaS cards
   - full-width asymmetrical flow with a systems-atlas tone
4. Framed the case study around operational leverage:
   - CRM routing
   - launch blueprints
   - governance / QA
   - sector pressure
   - promotion arc
   - enterprise proof
   - asset loading plan

#### Actual Results

- LFi now has a parallel comparison route that can be judged on narrative, layout, and authority without disturbing the live dossier.
- The new direction expands the visual language of the portfolio instead of repeating the Research Engine scheme.

---

### 2026-03-13 - Project Dossiers Placeholder Language Cleanup

**Commit:** `working tree (uncommitted)`

#### Necessity

Once the blog was retired, the project dossiers became the only public editorial surface of the portfolio. Several pages were still exposing workshop language such as "placeholder", "replace later", "reserve these blocks", or internal repo references. That language weakened authority even when the actual case-study structure was sound.

#### Process

1. Rewrote visible proof-layer sections across the active dossiers so they now read like editorial/product proof requests instead of internal TODO notes:
   - `project-portfolio-ibaifernandez.template.html`
   - `project-myboard.template.html`
   - `project-enterprise-crm.template.html`
   - `project-the-research-engine.template.html`
   - `project-elm-st.template.html`
   - `project-aglaya.template.html`
2. Normalized labels such as:
   - `Placeholder Proof Assets` → `Proof Layer`
   - `Placeholder Video/Image` → `Video Proof` / `Visual Proof` / `Technical Proof`
   - `Drop-in map for final media` → `Media Architecture For The Sales Layer`
   - `Reserve These Blocks For YouTube Showcase Embeds` → `YouTube Showcase Sequence`
3. Removed explicitly internal-facing wording:
   - MyBoard stage caption no longer talks about designed placeholders.
   - Elm St. no longer mentions that the source repo contains local video files.
   - AGLAYA proof blocks now use "suggested proof asset" language instead of "reserved proof asset".
4. Removed one stale hardcoded claim from the portfolio dossier proof manifest (`29 Playwright tests`) and replaced it with a future-proof regression-suite label.
5. Corrected the MyBoard runway sequence so it no longer skips from `Phase 2` to `Phase 4`.
6. Rebuilt generated pages and revalidated quality + E2E.

#### Actual Results

- The project pages now sound more like premium dossiers and less like in-progress production notes.
- The proof sections still preserve the asset plan, but without breaking trust for a real visitor.
- Validation after the cleanup pass:
  - `npm run build:pages`
  - `npm run test:quality`
  - `npm run test:e2e` (`24 passed`)

---

### 2026-03-13 - Blog Retired From Public Product Surface

**Commit:** `working tree (uncommitted)`

#### Necessity

The portfolio product direction is now explicit: projects are the editorial surface and there will be no public blog. Keeping `blog.html` alive created SEO noise, documentation drift, and a low-value public URL that did not match the actual product.

#### Process

1. Removed the blog page from the build pipeline:
   - deleted `src/pages/blog.template.html`
   - removed `blog.html` generation from `scripts/build-pages.mjs`
   - removed the committed generated `blog.html`
2. Retired blog-specific runtime/test artifacts:
   - deleted `tests/e2e/blog.spec.js`
   - deleted `src/components/index/blog-card.template.html`
   - removed blog-specific a11y/keyboard expectations from Playwright
3. Added permanent legacy redirects:
   - `/blog`
   - `/blog.html`
   both now resolve to `/` in `netlify.toml`
4. Mirrored that redirect behavior in `scripts/static-server.mjs` so local E2E matches production behavior.
5. Removed `blog.html` from:
   - `sitemap.xml`
   - `llms-full.txt`
   - print-PDF generation targets
   - performance budget config
   - QA docs / README / AGENTS / architecture and runbook references
6. Tightened the internal link checker so its default pass now scans all generated HTML pages in the root, not just home plus blog.
7. Removed two dead commented `href="#"` remnants from the home source components (`sidebar` + `hero`) so quality checks no longer report placeholder anchors after the blog retirement pass.

#### Actual Results

- The blog is no longer a generated or indexed public surface.
- Legacy `/blog` traffic has a controlled path back to the home page.
- CI/test/docs/build now describe the real product more honestly.
- Validation after the final cleanup pass:
  - `npm run build:pages`
  - `npm run test:quality`
  - `npm run test:e2e` (`24 passed`)

---

### 2026-03-05 - Project Pages Contact Strip Removed (Form + Contact Data)

**Commit:** `working tree (uncommitted)`

#### Necessity

Project pages should remain focused on the dossier/VSL narrative and avoid re-introducing the global contact form + location/phone/email block at the end of each case.

#### Process

1. Created a project-specific footer component with only copyright:
   - `src/components/project/footer-minimal.html`
2. Updated all project templates (`project-*.template.html` + `project.template.html`) to:
   - remove `@include ../components/project/contact-form.html`
   - replace `@include ../components/index/footer.html` with `@include ../components/project/footer-minimal.html`
3. Rebuilt generated pages and reran gates.

#### Actual Results

- All project pages now finish at project nav + minimal footer (no contact form, no location/phone/email panel).
- Home kept its existing contact surface unchanged in that pass; the blog surface was retired later.
- Pipeline remains green (`build`, `quality`, `e2e`).

---

### 2026-03-05 - Research Engine VSL Proportion Polish (Golden Rhythm Pass)

**Commit:** `working tree (uncommitted)`

#### Necessity

After the hard reset, the visual language was correct but the composition still needed finer proportion control (section rhythm, asymmetry balance, type scale continuity, and CTA cadence).

#### Process

1. Refined `researchengine_stitch_*` spacing tokens in `assets/css/style.css` to standardize left/right asymmetry and vertical flow.
2. Tuned headline/subtitle/body scales and line lengths for editorial rhythm while keeping the dark brutalist direction intact.
3. Reduced residual rigidness in content modules:
   - staggered feature columns,
   - improved risk-column balance,
   - refined featured-offer emphasis without reverting to card-heavy framing.
4. Improved FAQ interaction readability (custom summary marker + stronger hierarchy).
5. Preserved the visual constraints:
   - no light backgrounds,
   - no generic centered SaaS container behavior,
   - cyan reserved for strategic emphasis and primary CTAs.
6. Added responsive harmony adjustments (tablet/mobile) so staggered desktop composition collapses cleanly without breaking rhythm.
7. Mitigated YouTube embed stability issues by switching to `youtube-nocookie` and explicit `referrerpolicy="origin"` on the VSL iframe.

#### Actual Results

- Layout now feels more harmonic and less mechanically stacked.
- Hero, VSL block, and section transitions have cleaner editorial pacing.
- The page keeps the intended dark-brutalist identity while improving scanability.

---

### 2026-03-05 - Dark Brutalism Hard Reset for Research Engine VSL

**Commit:** `working tree (uncommitted)`

#### Necessity

The page still read as a boxed template instead of a premium sales surface. The design target was restated with strict constraints: no centered SaaS container rhythm, no card-heavy framing, solid abyss-black canvas, colossal typography, asymmetric spacing, and a single cyan accent hierarchy.

#### Process

1. Rebuilt `src/pages/project-the-research-engine.template.html` to remove Bootstrap `container/row/col` wrappers for this route and keep a direct full-width section flow.
2. Reworked `assets/css/style.css` (`researchengine_stitch_*` block) to enforce:
   - true dark-canvas background inheritance on page wrappers,
   - left/right asymmetric section offsets with large negative-space zones,
   - oversized uppercase headline system with tight tracking,
   - subdued secondary copy color and cyan used only for strategic emphasis,
   - border-light hierarchy separators instead of card stacks,
   - dark glassmorphism treatment for the VSL frame (`backdrop-filter` + cyan ambient glow).
3. Kept event instrumentation and CTA tracking unchanged.
4. Rebuilt generated outputs and re-ran checks.

#### Actual Results

- Visual direction is now materially closer to the required dark-brutalist/editorial VSL.
- Layout flow is fluid and asymmetrical, with reduced boxed scaffolding.
- CI-equivalent local gates remain green:
  - `npm run build:pages`
  - `npm run test:quality`
  - `npm run test:e2e` (29/29)

---

### 2026-03-05 - Fluid Contrast Fix for Research Engine (Seamless Pass)

**Commit:** `working tree (uncommitted)`

#### Necessity

The previous pass still looked too boxed and had poor visual contrast because project wrapper defaults were leaking light backgrounds into the VSL page. The target was a seamless long-form surface with stronger readability and fewer container borders.

#### Process

1. Overrode project wrapper defaults for this route in `assets/css/style.css`:
   - force transparent project wrapper background for this page,
   - remove extra container boxing and reset section layout to full-flow.
2. Added a dedicated fluid polish layer to `researchengine_stitch_*` styles:
   - reduced heavy section borders/background cards,
   - switched to continuous spacing rhythm and full-width composition,
   - kept only strategic dark modules (risk cards / offers) where hierarchy needs friction.
3. Improved headline and paragraph contrast and adjusted typography scale for desktop/mobile readability.
4. Rebuilt and validated (`build`, `quality`, `e2e`).

#### Actual Results

- Contrast is restored against a dark canvas.
- The page now reads more “seamless” and less boxed.
- Automated quality gates remain green.

---

### 2026-03-05 - Stitch-Faithful Research Engine Reframe + Project Sidebar Hidden on Desktop

**Commit:** `working tree (uncommitted)`

#### Necessity

The prior implementation still looked like a styled dossier, not like the provided stitch reference. The target was a closer direct-response VSL composition (centered hero, stronger typographic hierarchy, dominant video frame, staggered cards, and offer-first layout). The project sidebar behavior also needed an explicit correction: hide it on desktop in project pages, not compact it.

#### Process

1. Updated `src/components/project/sidebar.html` to tag the project-only sidebar wrapper (`project_sidebar`) without touching the homepage sidebar component.
2. Replaced compact behavior with desktop hide behavior in `assets/css/style.css`:
   - `project_sidebar` is now hidden on desktop breakpoints,
   - project content wrapper resets to `margin-left: 0`.
3. Rebuilt `src/pages/project-the-research-engine.template.html` using a stitch-faithful VSL structure:
   - centered protocol header and oversized uppercase headline,
   - dominant VSL frame with play-overlay treatment,
   - risk cards, infrastructure grid, and vertical deployment sequence,
   - offer-stack section with featured `Complete` tier emphasis,
   - FAQ and placeholder legend kept for production asset swap.
4. Added a dedicated `researchengine_stitch_*` style system in `assets/css/style.css` (dark base + cyan accent) while preserving existing analytics events and CTA tracking hooks.
5. Rebuilt generated assets/pages and validated gates (`build`, `quality`, `e2e`).

#### Actual Results

- The Research Engine page now follows the intended stitch visual direction much more closely.
- Desktop project pages no longer render the left sidebar, maximizing dossier width and focus.
- Quality guards and Playwright remain green after the structural rewrite.

---

### 2026-03-04 - GA4 Intent Instrumentation for The Research Engine Sales Page

**Commit:** `working tree (uncommitted)`

#### Necessity

The sales-page rewrite needed full intent telemetry, not only generic `cta_click` events. We needed specific event names for video consumption milestones, tier exploration, FAQ engagement, checkout intent, and deep-scroll buying behavior.

#### Process

1. Added explicit tracking attributes in `src/pages/project-the-research-engine.template.html` for:
   - VSL click and checkout click CTAs
   - tier-card view tracking
   - tier selection tracking
   - FAQ expansion tracking
2. Extended `assets/js/custom.js` analytics handling to support:
   - `data-track-event-secondary` (dual event dispatch on the same click)
   - view-based tracking via `IntersectionObserver` for tier visibility
   - FAQ open events (`research_engine_faq_open`)
   - page-depth milestones (`research_engine_scroll_50`, `research_engine_scroll_90`)
3. Added YouTube Iframe API telemetry for VSL milestones:
   - `research_engine_vsl_play`
   - `research_engine_vsl_progress_25`
   - `research_engine_vsl_progress_50`
   - `research_engine_vsl_progress_75`
   - `research_engine_vsl_complete`
4. Rebuilt generated assets/pages and re-ran quality + E2E gates.

#### Actual Results

- GA4 now receives intent events across the whole sales flow, not only button clicks
- The Research Engine page can be analyzed like a real conversion surface (watching, considering, selecting, buying intent)
- Existing analytics coverage remains intact (no regressions in automated tests)

---

### 2026-03-04 - The Research Engine Rewrite: From Case Dossier to Conversion-First Sales Page

**Commit:** `working tree (uncommitted)`

#### Necessity

The first implementation still behaved too much like a "project dossier". The target was stricter: a real VSL sales page that proves productization, hard-copywriting execution, and funnel logic under direct-response standards.

#### Process

1. Added a ninth project to the public roster in `content/projects.json`:
   - `project-the-research-engine.html`
   - dedicated template `src/pages/project-the-research-engine.template.html`
   - home-card copy + translation keys in `en.json` and `es.json`.
2. Replaced the dossier narrative with a conversion sequence:
   - hard hook + risk framing
   - PAS section (problem / agitation / solution)
   - mechanism explanation
   - VSL block with repeated checkout CTA
   - category/product stack
   - offer tiers + objection handling FAQ
   - final close CTA
3. Built the sales copy from the source marketing docs (`biz plan`, `sales main copy`, `tiers`, `manual`, `marketing plan`) while keeping claims verifiable.
4. Added placeholder legend blocks (`Asset Slot A1-A5`) so final media can be dropped in exactly where the conversion narrative expects it.
5. Kept the product facts anchored to source material:
   - 53 raw prompts audited,
   - final 55-workflow package,
   - 5-category structure,
   - tiered commercial model (`Free`, `$9`, `$19`, `$49`).
6. Added a live VSL + commerce surface:
   - YouTube VSL embed (`N0sUEMesXYM`)
   - direct Gumroad CTA (`the-research-engine-ai-prompt-pack`).
7. Introduced dedicated visual language in `assets/css/style.css`:
   - `project_spotlight_mockup--researchengine`
   - full `researchengine_dossier_*` style system with responsive rules.
8. Extended operational/discovery surfaces for the new route:
   - `scripts/generate-print-pdf.mjs`
   - `sitemap.xml`
   - `llms.txt`
   - `llms-full.txt`
   - `docs/QA-DESKTOP.md` and `docs/QA-MOBILE.md`
   - `docs/ROADMAP.md` and `docs/BACKLOG.md`.

#### Actual Results

- The page now behaves like a sales asset first, portfolio proof second
- Copy, offer stack, and CTA cadence are aligned with direct conversion intent
- Placeholder media positions are explicitly mapped for rapid asset replacement
- The public roster is now 9 dossiers
- Discovery, QA scope, and print generation are synchronized with the new route

---

### 2026-03-03 - Documentation Consolidation: One Status Story Instead Of Five Conflicting Ones

**Commit:** `working tree (documentation consolidation)`

#### Necessity

The project had reached a strong late pre-release state, but the documentation layer was still making it look more chaotic than it really was. Multiple files were repeating the same status with different wording, and some of them were still describing already-resolved items as if they were open.

#### Process

1. Reframed `docs/ROADMAP.md` as the single source of truth for:
   - current state,
   - release path,
   - what remains,
   - explicit release decisions.
2. Reduced `docs/BACKLOG.md` to active open work only, moving historical context out of the task list.
3. Reduced `docs/DEPLOY_ROADMAP.md` to a bridge document so it no longer competes with `ROADMAP`, `BACKLOG`, or the runbook.
4. Synced stale support docs that were still out of date:
   - `docs/SECURITY.md`
   - `docs/ARCHITECTURE.md`
   - `docs/GLOSSARY.md`
   - `docs/ENGINEERING-RUNBOOK.md`
   - `docs/QA-DESKTOP.md`
   - `docs/QA-MOBILE.md`
   - `docs/PRD.md`
5. Made the release posture explicit:
   - Turnstile is validated in production
   - manual QA is recommended but currently deferred by the owner
   - Search Console / Bing verification can proceed before QA if desired
   - `sitemap.xml` submission is better after content freeze
   - CSP enforce mode is intentionally deferred beyond `v2.0.0.0`

#### Actual Results

- The repo now has a much clearer documentation hierarchy
- Open work is easier to identify without reading the same information in multiple places
- Resolved items are less likely to be misread as blockers
- Human readers and AI agents now have a cleaner path from "where we came from" to "where we are" to "where we go next"

---

### 2026-03-03 - AGLAYA Dossier: Conversion-First Remote Agency Case

**Commit:** `see Git history after merge to main`

#### Necessity

`AGLAYA` was still using the generic fallback project template, which flattened one of the portfolio's strongest business-design cases into a content stub. The live agency already has a dense public narrative, but the wrong part to emphasize here would have been the education branch, because that angle is already properly documented in the separate `2x2MKT` dossier.

#### Process

1. Re-read the live `aglaya.biz` site and extracted the signals that matter for this portfolio:
   - anti-conformist positioning,
   - the free visual-identity-test CTA,
   - the fully remote agency model,
   - the four strategic methods,
   - the staged service architecture for brand birth, consolidation, and expansion.
2. Replaced the generic fallback with a dedicated `src/pages/project-aglaya.template.html`.
3. Framed the page around the commercial operating system:
   - qualification-first messaging,
   - conversion-oriented UX,
   - remote delivery logic,
   - brand strategy tied explicitly to sales and positioning.
4. Explicitly excluded the education branch from the center of the dossier and linked that narrative back to `The Route to Digitalization / 2x2MKT`, preventing content duplication between the two cases.
5. Added reserved proof placeholders for future:
   - brand-audit / evaluation captures,
   - portfolio montage proof,
   while still making the page indexable and semantically descriptive today.

#### Actual Results

- `AGLAYA` now reads like a real agency-systems case instead of a generic placeholder route
- The portfolio can now showcase this brand as a conversion architecture project, not just as a broad "innovation ecosystem"
- The content avoids duplicating the training narrative already covered in `2x2MKT`
- The page is structurally ready for richer media without blocking launch sequencing

---

### 2026-03-03 - Elm St Dossier: Cinematic Case Study With Reserved YouTube Embed Slots

**Commit:** `working tree (uncommitted)`

#### Necessity

`Elm St` was still falling back to the generic project template, which wasted one of the strongest narrative opportunities in the portfolio: a custom audiovisual studio website already backed by a real source repository, real team structure, and a real categorized body of work.

#### Process

1. Inspected the real `elm-st-web` source repository to extract verifiable project signals:
   - audiovisual studio positioning,
   - the "Si se ve o se escucha, lo hacemos" promise,
   - the multi-page structure (`inicio`, `nosotros`, `portafolio`, `contacto`),
   - the 18-piece categorized portfolio,
   - the three-person creative leadership surface.
2. Replaced the generic fallback with a dedicated `src/pages/project-elm-st.template.html`.
3. Framed the dossier with a restrained cinematic visual system:
   - dark surfaces,
   - warm accent highlights,
   - video-first proof blocks,
   - stronger mood without turning it into a costume theme.
4. Added explicit YouTube embed placeholders for the reel and three named pieces (`Noche sin Mañana`, `2x2`, `JIT`) so the content structure is ready before the final embeds are dropped in.
5. Updated the project entry so `Elm St` now generates from its own dedicated template instead of the generic placeholder page.

#### Actual Results

- `Elm St` now reads like a real case study instead of a "content pending" stub
- The dossier is grounded in actual source material from the original site repo
- The page is structurally ready for future YouTube embeds without blocking content and indexing work
- The visual direction is cinematic, but controlled enough to stay aligned with the stronger modern dossiers

---

### 2026-03-03 - LFi Dossier Modernization (Modern Systems Narrative + Newspaper Retirement)

**Commit:** `working tree (uncommitted)`

#### Necessity

The LFi case still carried a strong "newspaper" execution that had become stylistically off-target for the final portfolio. The underlying business case remained strong, but the presentation was selling a visual gag instead of the real value: operational systems thinking under enterprise pressure.

#### Process

1. Rebuilt `src/pages/project-enterprise-crm.template.html` from the ground up as a modern dossier:
   - removed the dedicated `lfi-newsprint` stylesheet include,
   - removed the external Google Fonts dependency used only by the old newspaper look,
   - replaced the legacy masthead / double-column editorial treatment with a structured systems case-study layout.
2. Reframed the narrative around the actual leverage:
   - agency operating infrastructure,
   - deterministic CRM routing,
   - reusable launch blueprints,
   - promotion arc driven by systems design.
3. Updated the home project card to stop using the fake newspaper cover and use the real LFi operations capture (`lfi-la`) as the canonical media surface.
4. Removed the old `lfi-newsprint` asset pipeline dependency from:
   - `scripts/build-pages.mjs`
   - `tests/quality-guards.sh`
   - the print stylesheet assumptions.
5. Retired the newspaper-only assets that no longer belong to the live stack.

#### Actual Results

- The LFi page now matches the visual and narrative direction of the stronger modern dossiers
- The old newspaper concept is no longer part of the active production path
- The card and page now sell real operational authority instead of a novelty aesthetic
- The asset pipeline is simpler because `lfi-newsprint.css` is gone

---

### 2026-03-03 - MyBoard Dossier: Product Case Study With Enterprise Runway

**Commit:** `working tree (uncommitted)`

#### Necessity

`MyBoard` is no longer just an internal side project mention. Its own repo already proves a clear product thesis: zero vendor lock-in, local-first ownership, modular React + Express architecture, and a roadmap toward enterprise and AI-assisted workflows. It needed a proper dossier in the portfolio.

#### Process

1. Added a new public project route: `project-myboard.html`.
2. Added `MyBoard` to `content/projects.json`, making it part of the home showcase and generated project navigation.
3. Wrote a dedicated case-study template using only verifiable material from the `MyBoard` repository docs:
   - React 18 + Vite frontend
   - Express backend
   - JSON persistence in `tasks.json`
   - `@dnd-kit` drag and drop
   - Tailwind UI layer
   - multi-board, categories, filters, and roadmap through AI-assisted Phase 4
4. Added placeholder proof media blocks so the dossier is structurally complete before the real screenshots and video are dropped in.
5. Updated discovery files and print-PDF targets so the page behaves as a first-class public asset.

#### Actual Results

- `MyBoard` now exists as an explicit product case study inside the portfolio
- The home project showcase reaches the intended 8-dossier launch set
- The dossier already contains indexable narrative, technical framing, and semantic placeholder media labels
- The remaining upgrade path is simple: swap placeholder proof blocks for real captures

---

### 2026-03-03 - Portfolio Dossier: Dedicated Case Study With Placeholder Proof Media

**Commit:** `working tree (uncommitted)`

#### Necessity

The portfolio itself had become a substantive engineering case study: platform migration, CI/CD hardening, accessibility work, performance improvements, production validation, and release governance. It needed to exist as a first-class project dossier instead of only being implied through the homepage.

#### Process

1. Added a new public project route: `project-portfolio-ibaifernandez.html`.
2. Added the portfolio to `content/projects.json`, so it now appears in the home project showcase and participates in generated project navigation.
3. Wrote a dedicated project template with content grounded in the current repo state: Netlify migration, test coverage, PageSpeed results, anti-spam contract, and production validation.
4. Introduced placeholder media blocks for the proof assets that should exist later (walkthrough video, before/after evidence, launch proof captures) without blocking the content build.
5. Updated discovery files (`sitemap.xml`, `llms.txt`, `llms-full.txt`) and print-PDF targets so the new dossier is treated as a first-class public page.
6. Re-generated the Playwright visual baselines for the Home projects/logos section because the grid height changed intentionally when the new project card entered the roster.

#### Actual Results

- The portfolio is now represented as an explicit, sellable project case study
- The copy is grounded in the real engineering work already completed in this repository
- Placeholder media exists where richer screenshots or videos should later be substituted with real assets
- The project roster now moves closer to the intended 8-dossier launch set

---

### 2026-03-03 - Manual QA Runbooks: Desktop + Mobile

**Commit:** `working tree (uncommitted)`

#### Necessity

The project is approaching launch-hardening. Automated tests are already strong, but there was no explicit human QA gate documented for desktop and mobile before Search Console / Bing onboarding.

#### Process

1. Added a dedicated desktop release checklist in `docs/QA-DESKTOP.md`.
2. Added a dedicated mobile release checklist in `docs/QA-MOBILE.md`.
3. Updated `docs/ENGINEERING-RUNBOOK.md` so the manual QA step is now part of the formal release flow.
4. Updated roadmap and backlog references so the QA documentation is tracked separately from the QA execution itself.

#### Actual Results

- The project now has an explicit human release gate for desktop and mobile
- QA can proceed in a controlled, repeatable way without waiting for final content freeze
- Launch sequencing is clearer: manual QA before Search Console / Bing

---

### 2026-03-03 - Home Cleanup: Remove Obsolete Hardcoded "Coming Soon" Projects Block

**Commit:** `working tree (uncommitted)`

#### Necessity

The homepage still contained a second, older projects-like section below testimonials. It was hardcoded and unrelated to the real data-driven project grid, which caused a misleading "Coming Soon" block to appear in production even though the actual project showcase was already rendered above it.

#### Process

1. Removed the obsolete hardcoded block from `src/pages/index.template.html`.
2. Left the real data-driven projects grid untouched (`content/projects.json` + `@render projects-grid` remains the source of truth).

#### Actual Results

- The homepage no longer shows a duplicate "Coming Soon" projects section
- The visible projects area is now the single canonical showcase driven by `content/projects.json`

---

### 2026-03-03 - Manual Production Validation: Contact Flow + GA4 Realtime Confirmed

**Commit:** `working tree (uncommitted)`

#### Necessity

The Turnstile UX hotfix needed a real production check because local E2E does not exercise Cloudflare Turnstile. The goal was to close the real production validation loop for the contact flow: UI behavior, inbox delivery, and analytics ingestion.

#### Process

1. Deployed commit `5a44afc` to production via the standard GitHub Actions pipeline.
2. Repeated a real contact-form submission on `https://portfolio.ibaifernandez.com`.
3. Observed the live UI behavior after submit.
4. Confirmed that both transactional emails arrived in the target inbox:
   - autoresponse (`Thank you for your message`)
   - owner notification (submitted subject line)
5. Confirmed GA4 Realtime ingestion in production:
   - `page_view`
   - `contact_submit_attempt`
   - `contact_submit_success`
6. Captured post-hardening PageSpeed results on production:
   - Desktop: `93 / 95 / 96 / 92`
   - Mobile: `62 / 95 / 92 / 92`

#### Actual Results

- Invisible Turnstile no longer introduces the visible white block / layout jump in the happy path
- The new success notice renders correctly and reads `Your message has been sent successfully.`
- Production contact UX is now visually stable for the normal submit path
- Real inbox delivery is confirmed end to end
- GA4 Realtime confirms both pageview and contact conversion events
- Contact flow production validation is now fully closed
- Launch-readiness metrics now have a fresh post-hardening baseline, not just the post-migration baseline

---

### 2026-03-03 - Public Slug Normalization: 2x2MKT / The Route To Digitalization

**Commit:** `working tree (uncommitted)`

#### Necessity

The public slug `project-national-tech-evangelism.html` was technically valid but editorially weak. It did not match the recognisable identity of the project (`The Route to Digitalization / 2x2MKT`) and was not a URL worth carrying into launch.

#### Process

1. Renamed the public output path to `project-ruta-digitalizacion-2x2mkt.html`.
2. Renamed the source template path to match the public slug.
3. Updated the project card title/copy to use the recognisable project name.
4. Added a Netlify `308` redirect from the old slug to the new slug, preserving backwards compatibility.
5. Updated generation scripts so print-PDF targets and social metadata maps follow the new slug.
6. Aligned static discovery files (`sitemap.xml`, `llms.txt`, `llms-full.txt`) to the new public URL.

#### Actual Results

- The project now has a recognisable launch-ready public URL
- Existing links to the old slug continue to resolve cleanly through Netlify
- The public project URL, redirects, discovery files, project nav, and print targets now point to the normalized slug consistently

---

### 2026-03-03 - Contact UX Hotfix: Invisible Turnstile Layout Shift + Feedback Styling

**Commit:** `working tree (uncommitted)`

#### Necessity

The first real production form submission exposed a UX regression in the contact flow:

- invisible Turnstile still caused a visible white placeholder block to appear during submit
- the captcha mount pushed the form layout just before completion
- the success feedback rendered as plain text with weak visual affordance

The backend behavior was correct, but the frontend experience felt unstable and unfinished.

#### Process

1. Updated the contact form templates so the response message renders before the captcha mount and the response itself sits inside a proper full-width Bootstrap column.
2. Changed the captcha mount behavior in `assets/js/custom.js`:
   - invisible Turnstile now mounts in a staged off-flow state by default
   - the script is warmed on first form-field focus instead of only at submit time
   - the widget is only brought back into the visible flow if a real interactive captcha retry is needed
3. Refined the response UI in `assets/css/style.css`:
   - success/error messages now render as styled notice cards
   - updated the success copy to `Your message has been sent successfully.`
4. Updated the contact E2E assertion to match the new success copy.

#### Actual Results

- Normal invisible Turnstile submissions no longer introduce a visible layout jump in the happy path
- The success/error state is clearer and more polished
- The form retains a recovery path for visible captcha interaction if Turnstile requests it

---

### 2026-03-03 - Infra Audit: Git Remote Parity + Netlify Runtime Hygiene

**Commit:** `working tree (uncommitted)`

#### Necessity

Before running production validation (real contact-form send, analytics realtime checks, manual QA), the repository and the live platform needed a fresh parity audit. The goal was to verify that:

- local `main` still matches GitHub,
- no local-only tooling artifacts are at risk of accidental commit,
- Netlify remains configured as a passive deploy target behind GitHub Actions.

#### Process

1. Refreshed remote refs with `git fetch --prune origin`.
2. Verified Git state:
   - `main` == `origin/main` at commit `0371ecf`
   - `origin/claude/affectionate-lovelace` was already fully merged into `origin/main`
   - removed the stale merged local branches `new-branch` and `claude/affectionate-lovelace`
   - removed the stale merged remote branch `origin/claude/affectionate-lovelace`
   - the historical tag `v2026.02.27-predeploy` is still present
3. Audited repository hygiene:
   - confirmed no tracked `.env`, key, PDF, Playwright artifact, or local tool state files
   - added `/.claude/`, `/.netlify/`, `.env`, and `.env.*` to `.gitignore` (while keeping `!.env.example` allowed)
4. Re-checked deploy configuration in:
   - `.github/workflows/ci.yml`
   - `netlify.toml`
   - `netlify/functions/contact.js`
5. Confirmed manually from the Netlify dashboard that:
   - the site shows **"Builds are stopped"**, so Netlify auto-builds are disabled and GitHub Actions remains the only production deploy gate
   - `RESEND_API_KEY`, `PORTFOLIO_CAPTCHA_PROVIDER`, and `PORTFOLIO_CAPTCHA_SECRET` are present in Netlify environment variables
   - Turnstile was therefore correctly reclassified from "not activated" to "awaiting live production validation" at that moment; the real production flow was later validated on 2026-03-03

#### Actual Results

- Local/remote parity is confirmed for `main`
- Only `origin/main` remains as an active remote branch
- The only local drift source was tooling state; it is now ignored by default
- No unexpected tracked secrets or generated artifacts were found
- No urgent repo-side Netlify configuration change is required before production validation
- Turnstile should no longer be documented as "not activated"; the remaining task is validating the real production flow end to end

---

### 2026-03-03 - CI Parity Hotfix: Platform-Independent PNG Alpha Detection

**Commit:** `working tree (uncommitted)`

#### Necessity

The expanded AVIF coverage checks were behaving differently on macOS and GitHub Actions Ubuntu runners. Locally, `sips` correctly identified alpha-channel PNGs and skipped them from AVIF requirements. In CI, `sips` does not exist, so those same PNGs were treated as non-alpha and incorrectly failed the quality gate.

#### Process

1. Replaced the OS-specific `sips` alpha detection in:
   - `tests/check-avif-coverage.mjs`
   - `scripts/generate-avif-assets.mjs`
   with an in-process PNG parser that inspects the PNG color type and `tRNS` chunk directly.
2. Adjusted the `experience-section.png` Playwright visual snapshot tolerance from `0.07` to `0.10` in `tests/e2e/visual.spec.js`. The section is text-heavy and showed a stable sub-10% font-rasterization delta between macOS and Ubuntu runners.
3. Re-ran:
   - `npm run test:quality`
   - `npm run test:e2e`

#### Actual Results

- AVIF alpha-PNG skipping is now deterministic across macOS and Linux
- `npm run test:quality` green again with the same expected skip count
- `npm run test:e2e` green (`29/29`)

---

### 2026-03-03 - Performance Sprint #4: Residual AVIF/WebP Closure Across All Generated Pages

**Commit:** `working tree (uncommitted)`

#### Necessity

The original media pipeline and its coverage guards were still centered on `index.html` and `blog.html`. That left a blind spot: project dossiers and `cv-print.html` could accumulate missing modern-image variants without CI noticing. Once the checks were expanded manually, two residual gaps surfaced:

- missing AVIF `<source>` tags in the enterprise CRM project testimonial ledger,
- missing WebP derivative for `marcelo-ricigliano.jpg`.

#### Process

1. Expanded the default scope of:
   - `tests/check-avif-coverage.mjs`
   - `tests/check-webp-coverage.mjs`
   - `scripts/generate-avif-assets.mjs`
   - `scripts/generate-webp-assets.mjs`
   - `scripts/wrap-images-with-avif-picture.mjs`
   so they now discover all generated root `*.html` files automatically.
2. Adjusted the standard NPM media workflow:
   - `media:avif` and `media:webp` now regenerate assets and then run `npm run build:pages`
   - this keeps the normal workflow aligned with the template-driven build instead of mutating generated HTML in place as the primary path
3. Closed the actual content gaps:
   - added missing AVIF `<source>` tags in `src/pages/project-enterprise-crm.template.html`
   - added the missing WebP `<source>` reference for Marcelo in `content/testimonials.json`
   - generated `assets/images/marcelo-ricigliano.webp` with a lower quality setting after the default encoder pass proved larger than the original source file

#### Actual Results

- `npm run test:quality` green with expanded media coverage
- `npm run test:e2e` green (`29/29`)
- Coverage snapshot after closure:
  - `AVIF checked=43`
  - `WebP checked=53`
  - `skipped_missing_asset=0`

---

### 2026-03-03 - Performance Sprint #3: Build-Generated CSS/JS Minification

**Commit:** `working tree (uncommitted)`

#### Necessity

After the accessibility debt was closed, the next direct performance lever was asset transfer size. The site was still serving readable source CSS/JS files in production (`style.css`, `font.css`, `animate.css`, `custom.js`, `translate.js`, etc.), which inflated the first-load CSS and JS budgets without delivering any runtime value.

#### Process

1. Extended `scripts/build-pages.mjs` so the build now does two things in one pass:
   - generates the committed HTML pages from templates/data
   - generates committed `.min` CSS/JS derivatives from the readable source assets and verifies them in `--check`
2. Updated all page templates to serve the generated `.min` assets by default (`*.min.css`, `custom.min.js`, `translate.min.js`, `cv-print.min.js`).
3. Updated `assets/js/custom.js` so deferred/lazy-loaded local assets also request `.min` variants where the build generates them.
4. Added regression guards in `tests/quality-guards.sh` so CI fails if generated HTML or lazy-load paths regress to legacy unminified references.

#### Actual Results

- `npm run build:pages` clean
- `npm run test:quality` green
- `npm run test:e2e` green (`29/29`)
- Home asset budget improved from:
  - CSS `410.3 KB` to `359.3 KB`
  - JS `144.4 KB` to `127.2 KB`

---

### 2026-03-03 - A11y Hardening: Re-enable Blocking `color-contrast`

**Commit:** `working tree (uncommitted)`

#### Necessity

`color-contrast` had remained intentionally non-blocking in `tests/e2e/a11y.spec.js` while the final contrast debt was still being burned down. After Sprint #2, the last remaining failing surface was the project spotlight CTA button: white text on a light orange background.

#### Process

1. Removed `color-contrast` from the non-blocking exception path in `tests/e2e/a11y.spec.js`, making it part of the default serious/critical failure set again.
2. Fixed the remaining failing component in `assets/css/style.css`:
   - `project_spotlight_cta` base state now uses the darker orange token
   - hover/focus uses the darker pink token
   - white text remains, now on compliant background colors
3. Re-ran:
   - `npm run test:e2e -- tests/e2e/a11y.spec.js`
   - `npm run test:quality`
   - `npm run test:e2e`

#### Actual Results

- `color-contrast` is blocking again in Playwright axe coverage
- `npm run test:e2e -- tests/e2e/a11y.spec.js` green
- `npm run test:quality` green
- `npm run test:e2e` green (`29/29`)

---

### 2026-03-03 - Performance + Accessibility Sprint #2: Heading Semantics + Contrast AA

**Commit:** `working tree (uncommitted)`

#### Necessity

The next a11y/performance tranche had two remaining low-complexity, high-leverage defects:

- Section blocks still used an invalid heading inversion (`h2.port_sub_heading` before `h1.port_heading*`), which weakens semantic structure for screen readers and search crawlers.
- Four vivid accent colors were still being reused as text/icon colors on white backgrounds. Visually they fit the brand, but they failed WCAG AA contrast for text.

#### Process

1. Reworked section heading markup in source templates and components:
   - `port_sub_heading` moved from `h2` to `p`
   - `port_heading*` moved from section-level `h1` to `h2`
   - applied in the home page section templates and the shared project contact block so generated project pages inherit the same semantics
2. Split the accent palette in CSS:
   - kept the existing vivid accent colors for decorative fills, backgrounds, and borders
   - introduced darker `*-text` tokens for yellow, pink, orange, and cyan
   - rewired text/icon surfaces (sidebar icons/tooltips, about headline, timeline accents, reusable accent utility classes) to use the text-safe variants
3. Added a regression guard to `tests/quality-guards.sh` so CI fails if generated pages ever reintroduce `h2.port_sub_heading` or `h1.port_heading*`.
4. Rebuilt all generated HTML pages and refreshed Playwright visual baselines impacted by the intentional palette shift (`projects-section`, `logos-section`).

#### Actual Results

- `npm run build:pages` clean
- `npm run test:quality` green
- `npm run test:e2e` green (`29/29`)
- Documentation updated to mark Sprint #2 closed and move minification + remaining AVIF/WebP work into the next sprint

---

### 2026-03-02 - Performance + Accessibility Sprint: Improvements #1–4 + Blog Cleanup

**Commit:** `ee6654d`

#### Necessity

PageSpeed Insights analysis run immediately after the Netlify migration DNS cutover revealed four high-impact, low-risk issues blocking better scores:

- **Mobile Performance: 61/100** (Desktop: 84/100). A portfolio showcasing engineering skills cannot afford a sub-65 mobile performance score. The gap between desktop and mobile pointed specifically to render-blocking resources, since layout and content are identical across devices.
- **Missing `<main>` landmark.** Lighthouse Accessibility flagged the absence of a landmark region wrapping the main content. The entire page content was wrapped in `<div class="port_sec_warapper">` — a generic container invisible to assistive technology. Screen-readers rely on landmark regions to let users jump directly to main content; without `<main>`, keyboard/AT navigation is degraded.
- **6 `href="#"` links in the blog section.** Lighthouse SEO flagged these as non-descriptive, non-navigable placeholder links. The blog section had been kept visible in the built `index.html` despite being commented out in the template (the build had not been re-run since the comment was added). Three cards contained `href="#"` links for "By Admin" and "50 comments" — SEO-dead anchors that crawlers treat as low-quality signals.
- **Render-blocking font chain (`@import` inside `font.css`).** `font.css` opened with `@import url('https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap')`. The browser must: (1) fetch and parse `font.css` (blocking), (2) discover the `@import` directive, (3) open a new blocking connection to `fonts.googleapis.com` and fetch the Roboto CSS, (4) discover `@font-face` entries pointing to `fonts.gstatic.com`, and (5) begin font file fetches. Steps 1–3 are fully sequential and block all painting. Two separate round-trips to two different origins before any pixel renders.
- **`animate.css` blocking render (~80 KB).** Loaded as a synchronous `<link rel="stylesheet">` in `<head>`. No element visible on initial viewport uses an animate.css class. The browser was blocking all painting until this 80 KB file was fully fetched and parsed on every page load.

#### Process

1. **Blog cleanup:**
   - Confirmed that the blog section (`port_blog_setions`, lines 644–753 in the template) was already commented out but still present in the built `index.html` from a stale build. The 6 `href="#"` links and `dummyimage.com` images were therefore visible in production.
   - Uncommented the section in `index.template.html`; replaced all 3 blog card interiors with clean "Coming soon…" placeholders — no images, no `href="#"` links, no fake dates, no Lorem Ipsum, no external dummy assets.
   - Updated section heading from "My Blog / Recent News" to "My Projects / Coming Soon".
   - Saved the original vertical card structure to `src/components/index/blog-card.template.html` for future project-page use (noted in comment header with usage instructions).

2. **`<main>` landmark:**
   - Changed `<div class="port_sec_warapper">` (template line 117) to `<main class="port_sec_warapper">` and its matching closing tag (template line 1016). No visual change — all styling is class-driven.
   - The `port_sec_warapper` class wraps every page section after the sidebar (about, experience, services, projects, skills, testimonials, blog, sponsors, contact, footer). Adding `<main>` here satisfies WCAG 2.1 SC 1.3.6 (Identify Purpose) and the Lighthouse "Page has a `<main>` region" audit.

3. **Font loading chain fix:**
   - Removed the `@import url(…)` from `assets/css/font.css` line 1. Added a comment explaining the change and why.
   - Added `<link rel="preconnect" href="https://fonts.googleapis.com">` and `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>` to the template `<head>`, immediately before the CSS link block. These pre-warm the DNS resolution and TLS handshake to both Google origins before any font request fires — independently of when the font stylesheet loads.
   - Added Roboto as a non-blocking preload: `<link rel="preload" as="style" onload="this.onload=null;this.rel='stylesheet'" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap">` with `<noscript><link rel="stylesheet" href="…"></noscript>` fallback. The browser fetches the font CSS in parallel with all other resources and applies it after first paint — zero blocking.

4. **`animate.css` defer:**
   - Changed `<link rel="stylesheet" href="assets/css/animate.css">` to `<link rel="preload" as="style" onload="this.onload=null;this.rel='stylesheet'" href="assets/css/animate.css">` with `<noscript>` fallback. The browser preloads the file at high priority (correct, since it will be needed soon) but does not apply it as a stylesheet until the `onload` fires — after first paint. `this.onload=null` prevents the handler firing again if the browser re-evaluates the attribute.

5. **Rebuild and validate:** `npm run build:pages` → `npm run test:quality` → `npm run test:e2e` — 29/29 green. Pushed to `main`; GitHub Actions CI green; Netlify deploy triggered automatically.

#### Expected Results

- **`<main>` landmark:** closes the Lighthouse Accessibility "page has no `<main>` region" flag entirely. Expected +1–2 points Accessibility on desktop and mobile.
- **Blog `href="#"` links:** closes the Lighthouse SEO "links are not crawlable" / "links do not have descriptive text" flags. Expected +1–3 points SEO on desktop and mobile.
- **Font chain:** eliminates one sequential blocking round-trip on the critical path. The browser now discovers and starts fetching the Roboto CSS in the same pass as all other `<head>` resources, with a pre-warmed connection. Expected FCP improvement of 200–400 ms on mobile (cold connection), contributing to the Performance score.
- **`animate.css` defer:** removes the largest single blocking resource from the critical path (~80 KB). Expected to be the highest-impact individual change for mobile FCP. Estimated contribution: 300–600 ms FCP reduction on mobile emulation.
- **Combined effect:** estimated +10–18 points Mobile Performance, +1–3 points Accessibility, +1–3 points SEO. Desktop should also improve but was already at 84, so the delta will be smaller.

#### Actual Results

- All 29 E2E tests pass. Build clean. No regressions.
- Deployed to Netlify. CDN propagation immediate.
- PageSpeed lab scores not re-captured immediately after deploy (CrUX field data requires 28-day collection window to update; Lighthouse lab run will reflect changes immediately but is not cached in PageSpeed's reporting layer until next crawl cycle). Post-improvement lab run is a pending action.

---

### 2026-03-02 - Performance: JSON i18n Cache Header Optimization

**Commit:** `dd4abf1`

#### Necessity

The `content/*.json` files — the i18n string sources for the EN/ES language switcher (`en.json`, `es.json`) — were served with `Cache-Control: public, max-age=0, must-revalidate`. In practice this means: on every visit after the initial load, the browser must send a conditional GET (with `If-None-Match` / `If-Modified-Since`) to the origin and wait for a response before it can serve the JSON. Even if the server responds 304 Not Modified in ~50 ms, that is a blocking roundtrip that delays language switching.

With Netlify hosting, these files only change when a new deploy is pushed — and Netlify automatically invalidates its CDN cache on every deploy. The `max-age=0` was producing unnecessary network traffic with no correctness benefit.

#### Process

- Changed `netlify.toml` `/*.json` headers from `Cache-Control: public, max-age=0, must-revalidate` to `Cache-Control: public, max-age=86400, stale-while-revalidate=604800`.
- `max-age=86400` (1 day): browsers serve from local cache for up to 24 hours without any network request.
- `stale-while-revalidate=604800` (1 week): after the 1-day freshness window expires, the browser may serve the stale cached version immediately while revalidating in the background. Users never block waiting for a JSON file.
- Safety: Netlify CDN purge on deploy is the primary freshness mechanism. If JSON content changes (deploy), the CDN cache is instantly invalidated and all subsequent requests fetch the new version. Browser cache can hold a stale copy for up to 1 day in edge cases, but for a portfolio i18n file this is acceptable — the worst case is a user sees the previous i18n strings for up to 24 hours before their cache refreshes, which is not a correctness problem.

#### Expected Results

- Repeat visits: 2–4 fewer conditional GET roundtrips per session for users with the language switcher active.
- Reduced perceived latency on language switch after the cache is warm.
- No stale content risk: Netlify CDN invalidation on deploy + 1-day browser max-age provide two independent freshness layers.

#### Actual Results

- No regressions. Validated with `npm run test:quality`.
- Cache headers correct in `netlify.toml`. Netlify applies them as CDN response headers.

---

### 2026-03-02 - E2E Compatibility Fix: Static Server Post-Migration

**Commit:** `321ea7b`

#### Necessity

After the Netlify migration, the contact form frontend (`assets/js/custom.js`) was updated to POST to `/.netlify/functions/contact` with a JSON body (`Content-Type: application/json`). However, `scripts/static-server.mjs` — the lightweight Node.js HTTP server used to serve the site during Playwright E2E tests — had not been updated to match. It had two incompatibilities:

1. **Missing route:** it only handled `/ajax.php`. Requests to `/.netlify/functions/contact` returned a 404.
2. **Missing JSON body parser:** `parseFormFields()` handled `multipart/form-data` and `application/x-www-form-urlencoded` bodies, but silently returned an empty field map for `application/json` bodies. Even if the route had been present, the function would have received empty fields and rejected the submission.

Net effect: every E2E contact form test received a 404 or a rejection, the frontend showed "Something went wrong", and the CI suite failed after the migration was otherwise complete.

#### Process

1. **Added JSON parsing branch** in `parseFormFields()` of `scripts/static-server.mjs`:
   ```javascript
   if (mimeType.includes('application/json')) {
     try {
       const parsed = JSON.parse(body);
       if (parsed && typeof parsed === 'object') {
         for (const [key, value] of Object.entries(parsed)) {
           fields.set(key, String(value ?? ''));
         }
       }
     } catch {}
     return fields;
   }
   ```

2. **Added route handler** for the new function URL alongside the existing `/ajax.php` route:
   ```javascript
   if (pathname === '/ajax.php' || pathname === '/.netlify/functions/contact') {
     return handleAjax(req, res);
   }
   ```
   Both routes call the same `handleAjax()` mock function, which simulates a successful form submission response — consistent with how the static server approximated `ajax.php`.

3. **Validated:** `npm run test:e2e` — 29/29 green.

#### Expected Results

- E2E contact form tests pass under the new `/.netlify/functions/contact` endpoint URL and `application/json` content-type.
- Legacy `/ajax.php` route continues to be handled for completeness.
- No behavior change in production — the static server is strictly a test fixture; production traffic routes to the real Netlify Function.

#### Actual Results

- 29/29 E2E tests green immediately after the fix.
- GitHub Actions E2E job green on subsequent push.

---

### 2026-03-02 - Platform Migration: cPanel → Netlify

#### Necessity

The portfolio was hosted on a cPanel shared server. This imposed a set of constraints that were increasingly at odds with the engineering standards established during the February componentization and hardening sprints:

1. **No automated CI/CD.** Deployment was mediated by `.cpanel.yml` (cPanel's Git integration) or manual FTP. There was no mechanism to run quality gates or E2E tests before a change reached production. A bug that passed local review could reach production unchecked.

2. **PHP-only serverless runtime.** The contact form (`ajax.php`) required a live PHP 7.4 interpreter on the server. cPanel's PHP configuration is shared, non-containerized, and subject to hosting provider changes. Node.js was not available in the cPanel serverless surface. Migrating the contact form to a more modern stack (rate limiting, captcha verification, structured logging) would have required staying in PHP indefinitely.

3. **Single-origin delivery (no CDN).** Static assets were served from one origin server. Visitors outside the server's geographic region experienced higher latency for every asset — HTML, CSS, JS, images, webfonts. No edge caching, no geographic distribution.

4. **Fragile secrets management.** The email sending credentials (SMTP, or Resend API key post-migration) had to live in the PHP environment or in cPanel environment variables — a configuration surface shared with the hosting provider and harder to audit than a dedicated secrets store.

5. **Manual SSL renewal risk.** While cPanel can auto-renew Let's Encrypt certificates, the process is tied to the hosting provider's automation — failure to renew means outage. No independent control.

6. **No atomic deploys.** Files were overwritten in place during deploy. A partially-uploaded deploy state was observable by live users.

The decision to move to Netlify was driven by the need to close all six gaps simultaneously with a managed platform that required minimal operational overhead.

#### Process

**Phase 1 — Netlify site creation and `netlify.toml` authoring**

1. Created a new Netlify site linked to the `ibaifernandez/portfolio-ibaifernandez` GitHub repository via the Netlify GitHub App.
2. Authored `netlify.toml` with the following configuration:
   - **Build:** `command = "npm run build:pages"`, `publish = "."` (root — the site is pre-built static HTML; Netlify's build step just runs the template compiler), `functions = "netlify/functions"`.
   - **Node version:** `NODE_VERSION = "20"` in `[build.environment]`.
   - **Dev:** `command = "npm run start"`, `port = 4173` — allows `netlify dev` to serve the site locally with function proxying.
   - **Redirects:** `/ajax.php → /.netlify/functions/contact` (308 Permanent) — backward compatibility for any cached references to the old PHP endpoint.
   - **Headers:** security headers on `/*` (`X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options`, `Permissions-Policy`, `Cross-Origin-Resource-Policy`, `Content-Security-Policy-Report-Only`); cache control per asset type (HTML: no-cache; JSON: 1d + SWR 7d; CSS/JS: 7d; images: 30d; webfonts: 30d + immutable).

**Phase 2 — Contact form migration: `ajax.php` → `netlify/functions/contact.js`**

The PHP contact handler was completely replaced with a Netlify Function:

- **Runtime:** Node.js 20 CommonJS (`contact.js`), bundled by esbuild (Netlify's default function bundler).
- **Email transport:** Resend API. A single authenticated `POST https://api.resend.com/emails` replaces PHPMailer's SMTP handshake. Credentials: `RESEND_API_KEY` (Netlify environment variable), `FROM_EMAIL`, `TO_EMAIL`.
- **Anti-spam logic preserved in production:**
  - Honeypot field check (hidden field must be empty).
  - Minimum time-before-submit guard (form must be open for at least N seconds).
  - Cloudflare Turnstile captcha verification (reads `captcha_provider` and `captcha_token` from the POST body; verifies against Cloudflare's API using `PORTFOLIO_CAPTCHA_SECRET`).
- **Test parity:** `scripts/static-server.mjs` mirrors the production contact contract for local E2E coverage without adding protections that production does not have.
- **Frontend update:** `assets/js/custom.js` updated to POST to `/.netlify/functions/contact` with `Content-Type: application/json` body instead of the legacy `/ajax.php` form-encoded POST.
- **Bundler config:** `netlify.toml` `[functions]` section sets `node_bundler = "esbuild"` — Netlify bundles each function with its `node_modules` dependencies into a single file. No `package.json` inside `netlify/functions/` required.

**Phase 3 — GitHub Actions CI/CD**

1. Added `.github/workflows/quality.yml`:
   - Triggers on push to `main` and on PRs targeting `main`.
   - Steps: checkout → Node 20 setup → `npm ci` → `npm run build:pages` → `apt-get install ripgrep` (Linux CI dependency) → `npm run test:quality`.
   - Purpose: gate on build integrity and quality guards before Netlify sees the commit.

2. Added `.github/workflows/e2e.yml`:
   - Triggers on push to `main`.
   - Steps: checkout → Node 20 setup → `npm ci` → Playwright Chromium install → `npm run build:pages` → `npm run test:e2e`.
   - Purpose: gate on behavioral correctness before production deploy.

3. **Netlify deploy integration:** Netlify's GitHub App triggers a deploy on every push to `main`. The deploy runs after GitHub Actions (Netlify's deploy preview also runs on PRs). Netlify's deploy status check appears in the GitHub PR checks panel.

**Phase 4 — DNS transfer**

1. Domain registrar nameservers updated to Netlify's DNS (`dns1.p01.nsone.net` etc.).
2. Netlify DNS records configured: `A` record for `portfolio.ibaifernandez.com` pointing to Netlify's load balancer.
3. DNS propagation confirmed globally within 24–48 hours via `dig` and external DNS propagation checkers.

**Phase 5 — SSL**

- Netlify auto-provisions a Let's Encrypt certificate as soon as DNS propagation resolves. Certificate covers `portfolio.ibaifernandez.com` and `www.portfolio.ibaifernandez.com`.
- HTTPS enforced by Netlify CDN (HTTP requests receive 301 redirect to HTTPS). No application-layer configuration required.
- Renewal: automatic via Netlify/Let's Encrypt. No manual intervention required.

#### Expected Results

- **Automated CI/CD:** every push to `main` triggers quality + E2E gates. No deploy reaches production without passing the test suite. No manual FTP or `.cpanel.yml`-mediated uploads.
- **Serverless contact form:** no PHP server to maintain. Cold-start latency ~50–200 ms (acceptable for a contact form). Runtime updates handled by Netlify (no server patching).
- **CDN edge delivery:** static assets served from Netlify's global edge network (100+ PoPs). Reduced TTFB and asset latency for international visitors.
- **Automatic SSL:** certificate provisioned and renewed by Netlify/Let's Encrypt indefinitely, with no manual intervention or renewal risk.
- **Atomic deploys:** Netlify stages new files, then switches atomically. No partial deploy state observable by users.
- **Environment variable security:** secrets stored in Netlify's encrypted variable store, never committed to the repository.

#### Actual Results

- DNS propagation confirmed: ✅
- SSL auto-provisioned and HTTPS enforced: ✅
- GitHub Actions `quality` and `e2e` CI jobs green: ✅ (after `321ea7b` E2E fix)
- All 29 E2E tests passing in CI: ✅
- Contact form functional on production (Resend delivery confirmed): ✅
- Netlify deploy on git push operational: ✅
- **PageSpeed baseline captured immediately post-migration (before improvement sprint):**
  - Desktop: Performance **84** | Accessibility **94** | Best Practices **96** | SEO **92**
  - Mobile: Performance **61** | Accessibility **94** | Best Practices **96** | SEO **92**
  - These scores served as the baseline for the subsequent performance and accessibility improvement sprint.

---

### 2026-02-27 - National Route V2 (Bilingual + Normalized Media + Smoke Hardening)

1. Rebuilt `project-national-tech-evangelism` as fully bilingual (`EN/ES`) with real-time language switching:
   - page-level copy now syncs with `document.documentElement.lang`,
   - dynamic route simulator content (stop names, phase labels, KPI chips, controls) also switches language.
2. Normalized all National Route media naming to deterministic prefixes:
   - `rdld-press-*`
   - `rdld-photo-*`
   - `rdld-logo-*`
3. Added optimized modern formats for route evidence media:
   - generated `.webp` and `.avif` for all `rdld-press-*` and `rdld-photo-*`,
   - wired `<picture>` fallbacks in the project template for press/photo evidence blocks.
4. Hardened smoke test title assertion for CI portability:
   - replaced exact accented-title match with resilient pattern `<title>...Portfolio</title>` to avoid locale/encoding false negatives on Linux runners.
5. Validation:
   - `npm run build:pages` ✅
   - `npm run test:quality` ✅
   - `npm run test:e2e` ✅

### 2026-02-27 - AVIF Coverage Hotfix (CI Parity)

1. Fixed GitHub Actions quality failure caused by missing AVIF fallbacks in large home assets:
   - `assets/images/uma.avif`
   - `assets/images/debtracker.avif`
   - `assets/images/gymtracker-cover-2.avif`
2. Updated content sources so generated HTML includes deterministic AVIF `<source>` tags:
   - `content/training.json` (`uma` logo picture block),
   - `content/projects.json` (DebTracker and GymTracker spotlight cards).
3. Rebuilt generated pages to keep template sync:
   - `index.html`
   - `project-debtracker.html`
   - `project-gymtracker.html`
4. Validation:
   - `npm run test:quality` passed locally, including `check-avif-coverage`.

### 2026-02-27 - National Tech Evangelism Dossier (Interactive Route Wow)

1. Added a dedicated template for the National-Scale case:
   - `src/pages/project-national-tech-evangelism.template.html`
   - wired via `content/projects.json > page.template`.
2. Implemented an interactive "Route Simulator" module:
   - draggable/step-based mission marker (field -> cloud -> legacy),
   - phase-based dynamic briefing panel with live KPI chips,
   - autoplay mode with reduced-motion safeguards.
3. Added a standalone visual layer for this page:
   - `assets/css/national-route.css`
   - custom journey track, stop states, mission panel, and responsive behavior.
4. Updated page narrative blocks with the full roadmap copy:
   - Physical Frontier (NIC.ec),
   - Pandemic Pivot (2x2MKT),
   - Systemic Impact & Legacy.

### 2026-02-27 - CI Stabilization (GitHub Actions)

1. Fixed `quality.yml` runner bootstrap for Linux:
   - added system dependency installation for `ripgrep` before quality checks.
2. Root cause:
   - `tests/quality-guards.sh` depends on `rg`; missing binary in GitHub runner caused instant failures (`command not found`) and misleading downstream annotations.
3. Documentation updated:
   - runbook now includes `ripgrep` as explicit CI requirement and workflow behavior note.
4. Fixed README gating mismatch between local and CI:
   - removed `README.md` from `.gitignore`,
   - repository now tracks `README.md` so `tests/quality-guards.sh` can validate it consistently in GitHub Actions.

### 2026-02-27 - Print Fidelity Layer + Canonical PDF Export

1. Added dedicated print stylesheet:
   - `assets/css/print.css` with `@page` + `@media print` rules to avoid browser-default print degradation.
2. Wired print stylesheet in all page templates:
   - `index`, `blog`, base `project` template, and dedicated project templates.
3. Print-specific behavior hardening:
   - hides non-print UI (`sidebar`, `preloader`, toggles, floating controls),
   - forces expanded readability for experience cards and about details,
   - controls page-break behavior for dense cards/modules,
   - preserves color fidelity intent via `print-color-adjust`.
4. Added canonical PDF generation workflow:
   - `scripts/generate-print-pdf.mjs`
   - `npm run print:pdf`
   - outputs deterministic artifacts to `artifacts/print-pdf/` for recruiter-safe distribution.

### 2026-02-27 - Deploy Readiness Hardening (Pre-Release)

1. Deployment pipeline hardened in `.cpanel.yml`:
   - removed full-repository copy behavior,
   - now deploys only public artifacts (`index/blog/project pages`, `assets`, `ajax.php`, translations, discovery files).
2. Discovery infrastructure upgraded:
   - expanded `sitemap.xml` with home, blog, and all project pages,
   - reviewed `robots.txt` policy for crawlability and endpoint exclusions,
   - added `llms.txt` and `llms-full.txt` for machine-readable site discovery.
3. SEO/social metadata normalized:
   - canonical + OG + Twitter meta added across project templates and blog template,
   - homepage social URLs normalized to production domain.
4. GA4 tracking made cross-page consistent:
   - analytics snippet extracted to shared component,
   - included in home, blog, and all project templates.
5. Release governance documented:
   - added `docs/DEPLOY_ROADMAP.md` with missing-plan items, ordered execution, rollback, and QA Desktop v1.0 sheet.

### 2026-02-25 - Baseline Audit + Hardening Foundation

1. Full technical audit across frontend shell, content structure, and form backend.
2. Hardening goals defined:
   - avoid regressions,
   - keep visual identity stable,
   - raise production reliability.

### 2026-02-25 - Componentization Core

1. Phase 1: De-monolithization starts in `index.template`.
2. Phase 2: Projects/testimonials moved to data-driven rendering.
3. Phase 3: Education/Training timeline migrated to data-driven.
4. Phase 4: CTA blocks migrated to data-driven.

Outcome: content maintenance shifted from HTML hand-editing to controlled JSON sources and reusable components.

### 2026-02-25 - Performance + Accessibility Hardening

1. Lazy-loading strategy introduced for non-critical JS/CSS plugins.
2. Accessibility issues (serious/critical) closed in critical journeys.
3. Visual guardrails and budgets integrated in quality workflow.

### 2026-02-26 - Advanced Componentization And Composition

1. Experience and Services extracted into independent components.
2. Services fully data-driven at group and card level.
3. Experience composition made data-driven by rows/cards.

Outcome: section-level refactors can be made with low regression risk.

### 2026-02-26 - Content Reframes (Engineering-Safe)

1. Education narrative reframe with no structural regressions.
2. Core capabilities section retitled/reframed.
3. Services regrouped into architecture-phase logic.
4. Experience cards reframed one by one (AGLAYA, LFi tracks, NIC.ec tracks).
5. Elm St. experience added in chronological position.
6. Intro truncation-on-expand bug fixed for all experience cards.
7. Legacy brand experience removed without breaking layout consistency.

### 2026-02-26 - Projects Re-architecture

1. Legacy project filters removed.
2. Projects reduced to six strategic cases.
3. Layout redesigned to checkerboard matrix:
   - Row 1: image/text + text/image
   - Row 2: text/image + image/text
   - Row 3: image/text + text/image
4. Each project now links to a secondary local project page.
5. Project pages are generated from one template with navigation scaffolding:
   - `Back to portfolio`
   - `Prev` / `Next`
   - `All Projects`

### 2026-02-26 - DebTracker Dossier (Project Page V2)

1. Added per-project template override support in build pipeline:
   - `content/projects.json > page.template`
   - `scripts/build-pages.mjs` now uses default `src/pages/project.template.html` only when no override is set.
2. Added dedicated DebTracker project template:
   - `src/pages/project-debtracker.template.html`
   - includes contextual toggle (`Corporate View` vs `Dev Reality`) with runtime copy swap.
3. Added technical storytelling modules for recruiter-facing depth:
   - macOS-like code terminal with Prism.js syntax highlighting,
   - animated zero-trust flow block (`Client -> Server Action -> Firestore`),
   - protocol card grid for business-logic invariants.
4. Updated DebTracker card media in `content/projects.json`:
   - switched to `debtracker.webp` + PNG fallback.
5. Regenerated visual baseline for intentional projects-section changes:
   - `tests/e2e/visual.spec.js-snapshots/projects-section.png`
6. Added persistent attention cue to DebTracker context switch:
   - blinking color beacon over the switch to guide discovery of `Corporate View` / `Spite Driven`,
   - cue is persistent (no auto-dismiss logic).
7. Added interactive Penny-Perfect demo module:
   - visual split simulator with configurable `amount` and `splits`,
   - iterative remainder-cent rotation per click for deterministic walkthroughs,
   - sum check output to prove money conservation in every step.
8. Updated full `Spite Driven` narrative layer:
   - dedicated hostile/cynical copy for eyebrow, title, headings, body and protocol bullets,
   - `Spite Driven` label shown in toggle status,
   - `PT Sans` applied when `Spite Driven` mode is active.
9. Replaced visual arrow flow with a Zero-Trust security log simulator:
   - macOS-style terminal that streams backend/network validation logs in loop,
   - documents read-only client rejection (`403`) and server-authoritative success (`200`).
10. Reordered DebTracker technical modules to avoid stacked terminal blocks:
   - security logs terminal placed after architecture section,
   - Penny-Perfect source terminal moved below protocol cards.
11. Added language-aware Penny-Perfect code comments:
   - snippet comments auto-switch between EN/ES based on current `html[lang]`,
   - syntax highlighting re-applies after language changes.

### 2026-02-26 - GymTracker Dossier (Wow Modules v1)

1. Added dedicated GymTracker project template:
   - `src/pages/project-gymtracker.template.html`,
   - wired through `content/projects.json > page.template`.
2. Added module `Data Sovereignty Command Center`:
   - live local-only metrics (`0 outbound requests`, `cloud dependency none`, low-latency SQLite writes),
   - command-feed simulator for local-first telemetry.
3. Added module `QA Shield Wall`:
   - animated `97/97` counter, progress bar, and per-module pass blocks.
4. Added module `No Vendor Lock-In Proof`:
   - side-by-side SaaS vs GymTracker architecture comparison.
5. Upgraded GymTracker narrative with project-specific copy:
   - `Local-First Architecture / Python & SQLite`,
   - anti-bloat stack and deterministic AI pipeline paragraph,
   - production-ready QA paragraph.

### 2026-02-26 - Projects Fine Tuning (Sidebar + Media)

1. Project-page sidebar was standardized to match home sidebar behavior and look:
   - added shared component `src/components/project/sidebar.html`,
   - linked in `src/pages/project.template.html`,
   - linked in `src/pages/project-debtracker.template.html`,
   - linked in `src/pages/project-gymtracker.template.html`.
2. DebTracker card framing was tuned for better first impression:
   - added class hook in `content/projects.json`,
   - forced left-side image focus in `assets/css/style.css` (`object-position: left center`).
3. GymTracker card/media was switched from legacy placeholder to real project evidence:
   - replaced `cyberpocuro.*` media in `content/projects.json` with `gymtracker.png/.webp`,
   - optimized `assets/images/gymtracker.png` from `3600x2010` to `1800x1005` before web delivery.
4. Visual regression baseline was refreshed for intentional project-grid deltas:
   - updated `tests/e2e/visual.spec.js-snapshots/projects-section.png`.

### 2026-02-26 - LFi Dossier (Project Page V1, Editorial Narrative)

1. Added dedicated template for the third project case:
   - `src/pages/project-enterprise-crm.template.html`
   - wired through `content/projects.json > projects[2].page.template`.
2. Implemented an editorial/newspaper visual direction to highlight growth narrative:
   - masthead + byline shell,
   - lead story layout with drop cap intro,
   - pull-quote block for narrative emphasis.
3. Added recruiter-oriented impact modules:
   - KPI strip (`18+ accounts`, `4 sectors`, `weeks->hours`, `16 months trajectory`),
   - promotion timeline (`Aug 2023 -> Jun 2024 -> Dec 2024`),
   - sector coverage grid (`Banking`, `Energy`, `Retail`, `Real Estate`).
4. Added testimonial-ready placeholder block (`Testimonial Ledger`) prepared for next content import cycle.
5. Extended stylesheet with isolated `lfi_` namespace and responsive rules:
   - preserves existing project templates without style regressions.

### 2026-02-26 - Home Logos Update (Bill Capital)

1. Added `Bill Capital` to the main logos swiper in home:
   - source: `assets/images/logo-bill-capital.png`
   - destination link: `http://billcapital.com/` (hardened with `target="_blank"` + `rel="noopener noreferrer"`).
2. Optimized provided logo asset for web delivery:
   - resized from `600x625` to `240x250` to reduce payload while keeping retina-safe rendering in a `100x100` slot.

### 2026-02-26 - LFi Dossier V2 (Vintage Newsprint Aesthetic)

1. Rebuilt third project page visual language from modern editorial to true old-newspaper direction:
   - template rewritten in `src/pages/project-enterprise-crm.template.html`,
   - black-ink, paper-texture, high-density columns, masthead, and newswire hierarchy.
2. Replaced previous `lfi_editorial_*` stylesheet namespace with `lfi_newsprint_*`:
   - strict monochrome navigation and typography treatment,
   - halftone-like media treatment (`grayscale/contrast/sepia + blend`),
   - newspaper-style metric desk, trajectory wire, and sector board.
3. Preserved project navigation, sidebar behavior, and responsive stability while changing aesthetic direction radically.

### 2026-02-26 - GymTracker Cover Upgrade

1. Added new cover media for GymTracker card + project hero:
   - `assets/images/gymtracker-cover-2.png` optimized from `3600x2010` to `1800x1005`,
   - generated `assets/images/gymtracker-cover-2.webp`.
2. Updated `content/projects.json` to use `gymtracker-cover-2.*` in project card and hero media.
3. Added original `gymtracker.png` as internal evidence block inside GymTracker project page:
   - new figure module in `src/pages/project-gymtracker.template.html`,
   - style hooks in `assets/css/style.css` (`gymtracker_evidence_*`).

### 2026-02-26 - Operability + Visual Baselines

1. Playwright operability protocol hardened for orphan process handling.
2. Visual baseline coverage expanded for critical sections.
3. Baselines updated after intentional layout changes.

### 2026-02-26 - Workspace Hygiene Cleanup (Post-Antigravity)

1. Removed disposable prototype artifacts not used by production build:
   - deleted `sandbox/newspaper_example.html` and cleaned empty `sandbox/`.
2. Removed local runtime artifacts that should not persist in workspace:
   - deleted `playwright-report/` and `test-results/`.
3. Removed Finder metadata files (`.DS_Store`) across repository.
4. Hardened ignore rules to prevent re-pollution:
   - `.gitignore` now includes `**/.DS_Store` and `sandbox/`.
5. Revalidated after cleanup:
   - `npm run test:quality` passed,
   - `npm run test:e2e` passed (`29/29`).

### 2026-02-26 - LFi Dossier V3 (1898 Newsprint Direction)

1. Elevated the third project page to a stronger 1898 newspaper aesthetic:
   - switched typography stack to `Chomsky` (local masthead), `Playfair Display` (headlines), and `Libre Baskerville` (body),
   - wired local textures (`texture-newspaper-1/2/3`) and ornament overlays (`ornaments/*`) into the page surface and sheet treatment.
2. Expanded narrative modules in `src/pages/project-enterprise-crm.template.html`:
   - `Field Evidence Archive` with dated placeholder clippings (`Nov 2023`, `Jul 2024`, `Jan 2025`),
   - timeline upgraded with explicit KPI lines per promotion stage,
   - `Sector Press Board` converted to real logo wall with outbound links.
3. Replaced testimonial placeholder with evidence-backed recommendation cards:
   - `Franco Ogaz Palma`,
   - `Fernanda Munoz Onetto`,
   - `Bani Barranco`.
4. Added/optimized support assets:
   - generated `.webp` variants for textures and testimonial photos,
   - resized new logos `norden.png` and `clinica-costanera.png` for 100x100 carousel slots.
5. Home impact update:
   - added `Norden` and `Clinica Costanera` to the main logos swiper in `src/pages/index.template.html` with hardened external links.
6. Validation:
   - `npm run build:pages` passed,
   - `npm run test:quality` passed,
   - `npm run test:e2e` passed (`29/29`).

### 2026-02-26 - LFi Dossier V4 (Full-Width Newsprint + CSS Modularization)

1. Componentized the LFi page styling into a dedicated stylesheet:
   - moved all `lfi_newsprint_*` rules out of `assets/css/style.css`,
   - new page-scoped file: `assets/css/lfi-newsprint.css`,
   - linked only from `src/pages/project-enterprise-crm.template.html`.
2. Switched layout from centered boxed sheet to full-width editorial canvas:
   - removed max-width centering for the newspaper surface,
   - reduced hard card boxing and replaced with separator-led composition.
3. Improved readability and visual fidelity:
   - body copy reduced and moved from full justify to left-aligned flow,
   - added white-opacity overlay over newspaper texture to keep text legible,
   - retained vintage layering (paper grain + stains) with less visual noise.
4. Main hero media update:
   - replaced project lead image with `assets/images/lfi-la.png`,
   - optimized to `1800x1005` and generated `assets/images/lfi-la.webp`.
5. Testimonial language harmonization with runtime i18n:
   - testimonial meta + quotes now use `translate` keys,
   - added corresponding keys in `en.json` and `es.json`,
   - language toggle now switches this block consistently.
6. Validation:
   - `npm run build:pages` passed,
   - `npm run test:quality` passed,
   - `npm run test:e2e` passed (`29/29`).

### 2026-02-26 - Project Pages Footer Unification + Newsprint Contrast/Interactions

1. Unified project-page end block with shared contact form + footer icons sequence:
   - new component `src/components/project/contact-form.html`,
   - included in all project templates (`project.template`, `project-debtracker`, `project-gymtracker`, `project-enterprise-crm`),
   - contact form now appears before `src/components/index/footer.html` across all project pages.
2. Added spacing governance for project contact/footer transition:
   - new `project_contact_setions` rules in `assets/css/style.css` with responsive margins.
3. Refined LFi newsprint readability:
   - darkened non-black text tones to improve contrast against textured paper.
4. Added color-reveal interactions in LFi dossier:
   - main image returns to color on hover/focus,
   - testimonial avatars return to color on hover/focus,
   - sector logos return to color on hover/focus.
5. Validation:
   - `npm run build:pages` passed,
   - `npm run test:quality` passed,
   - `npm run test:e2e` passed (`29/29`).

### 2026-02-27 - LFi Dossier V5 (Timeline Readability + Ledger Expansion)

1. Updated newspaper masthead copy lock:
   - confirmed title is `The LFI Times` in `src/pages/project-enterprise-crm.template.html`.
2. Rebalanced main narrative density after dropping `Field Evidence Archive`:
   - expanded `The Intrapreneurial Pivot` with additional operating-layer protocol details,
   - expanded `Scale & Cross-Industry Impact` with adaptation/reliability paragraph.
3. Redesigned `Trajectory Wire` into a clearer visual timeline:
   - moved timeline module into main narrative column,
   - added vertical spine + node markers + stronger date/KPI hierarchy in `assets/css/lfi-newsprint.css`.
4. Expanded `Testimonial Ledger` for longer recommendations:
   - larger avatar/text grid and higher readability typography,
   - updated quote copy length in `en.json` and `es.json`.
5. Fixed testimonial avatar hover color reveal bug:
   - removed grayscale filter from parent `picture` container and scoped filter to `img` only.

### 2026-02-27 - LFi Dossier V6 (Newsprint Authenticity Pass)

1. Brand correction in masthead:
   - updated newspaper title to `The LFi Times` in `src/pages/project-enterprise-crm.template.html`.
2. Structural rollback requested by UX direction:
   - moved `Trajectory Wire` back to the right sidebar column,
   - moved `Testimonial Ledger` back to the right sidebar column.
3. Newspaper reading pattern reinforcement:
   - applied two-column prose flow to main narrative (`column-count: 2`, `column-gap: 2rem`) with responsive fallback to one column on tablet/mobile.
4. Authentic print treatment pass:
   - desaturated page paper tone to reduce parchment/map feel,
   - removed decorative top flourish behind header strapline,
   - applied exact print-like evidence filter (`grayscale(100%) contrast(120%) sepia(10%)` + `mix-blend-mode: multiply`) on lead evidence media.
5. Sidebar typography rebalance:
   - kept timeline visual spine/nodes while scaling text for right-column legibility,
   - tuned testimonial card sizing for sidebar width.

### 2026-02-27 - Projects Grid UX Link Surface + LFi Newspaper Cover

1. Increased click-surface UX in `Finest Work` cards:
   - project media block is now clickable (same destination as CTA),
   - implemented in component + build pipeline (`src/components/index/project-card.html`, `scripts/build-pages.mjs`),
   - keyboard focus ring added on media link (`assets/css/style.css`).
2. Replaced third project card media with the newspaper visual:
   - optimized `assets/images/lfi-newspaper.jpg` to `1800x1075`,
   - generated `assets/images/lfi-newspaper.webp`,
   - updated `content/projects.json` (Enterprise CRM card) to use the new image pair.

### 2026-02-27 - Home Testimonials Refresh (Franco + Fernanda Full Copy)

1. Updated home testimonials dataset in `content/testimonials.json`:
   - removed `Belén Guarnieri` from the carousel source,
   - added `Franco Ogaz Palma` and `Fernanda Muñoz Onetto` with full recommendation texts.
2. Added AVIF assets for new testimonial images to satisfy media quality guards:
   - `assets/images/franco-ogaz.avif`,
   - `assets/images/fernanda-munoz.avif`.
3. Added i18n keys for the new testimonials:
   - `franco-recommendation` and `fernanda-recommendation` in `en.json` and `es.json`.

## Consolidated ADR Decisions

### ADR-001 (Componentization Strategy)

Decision: move progressively from monolithic HTML to componentized + data-driven architecture while preserving static output behavior.

Rationale:
1. Lower long-term maintenance cost.
2. Safer refactor surface via smaller components.
3. Controlled content updates through JSON.

### ADR-002 (Design Tokens / CSS Governance)

Decision: centralize style governance in existing stylesheet conventions and enforce consistency through tests, not ad-hoc CSS scattering.

Rationale:
1. Minimize visual regressions.
2. Keep predictable cascade behavior in legacy-compatible environment.
3. Maintain performance discipline with explicit budgets.

### ADR-003 (Platform: Netlify over cPanel)

Decision: migrate hosting from cPanel to Netlify (2026-03-02).

Rationale:
1. CI/CD integration (GitHub Actions → `netlify-cli deploy`) eliminates manual deployment steps and introduces automated quality gates (`ci.yml`) in the production path. A commit that fails quality guards or E2E tests cannot reach production.
2. Netlify Functions (Node.js 20, esbuild-bundled) provide a modern serverless runtime for the contact form, removing the PHP maintenance dependency and enabling the Resend API migration.
3. Netlify's global CDN edge network provides low-latency delivery of static assets to international visitors without additional infrastructure configuration.
4. Automatic SSL provisioning (Let's Encrypt via Netlify) removes certificate renewal risk and manual intervention.
5. Atomic deploys eliminate partial-deploy visibility windows.
6. Netlify's encrypted environment variable store provides a cleaner secrets management model than cPanel environment variables.

Alternatives considered:
- **Vercel:** functionally equivalent for this use case. Rejected in favor of Netlify because `netlify.toml` (headers, redirects, functions) is more expressive for a multi-concern configuration (cache headers per path, security headers, legacy redirects) than Vercel's `vercel.json`.
- **GitHub Pages:** no serverless functions; would have required a separate API service for the contact form. No custom response headers without a 3rd-party Action. Rejected.
- **Cloudflare Pages:** viable alternative with better edge performance. The Cloudflare Workers runtime has historically differed from standard Node.js (no native `process.env`, Web APIs instead of Node APIs). Rejected to avoid porting the contact function to a different runtime. Revisitable in a future migration if performance data warrants it.
- **Self-hosted VPS (DigitalOcean, Hetzner):** full control, but introduces server maintenance (OS patches, Node.js upgrades, Nginx/Caddy config, certificate renewal) that is disproportionate for a single-page portfolio with one serverless function.

### ADR-004 (Contact Form Email Transport: Resend over SMTP/PHPMailer)

Decision: use Resend API as the email transport in the Netlify Function (2026-03-02).

Rationale:
1. **Serverless fit.** Resend is a REST API (`POST https://api.resend.com/emails` with JSON payload) — no SMTP handshake, no persistent TCP connection, no TLS negotiation in application code. This is the correct transport model for a stateless serverless function where each invocation is independent.
2. **Simplicity.** A single authenticated HTTP request replaces PHPMailer's multi-step SMTP session. Less code surface, fewer failure modes.
3. **Deliverability.** Resend supports custom sending domains with automatic SPF/DKIM/DMARC configuration. Emails from `FROM_EMAIL` (a verified Resend domain) are less likely to be flagged as spam than emails from a generic shared-hosting SMTP relay.
4. **Free tier sufficiency.** Resend's free tier (100 emails/day, 3,000/month) is more than adequate for a portfolio contact form.
5. **Secret management.** A single `RESEND_API_KEY` environment variable is the only secret required, stored in Netlify's encrypted variable store.

Alternatives considered:
- **PHPMailer over SMTP (existing):** PHP-only. Incompatible with Node.js Netlify Function runtime. Rejected.
- **Nodemailer + SMTP:** would require a stable SMTP host and credentials. Cold-start latency higher (TLS handshake per invocation). More complex configuration (host, port, auth, TLS options). Rejected.
- **SendGrid:** same category as Resend (REST API for transactional email). More complex SDK, stricter free tier (requires credit card for higher limits). Rejected in favor of simpler Resend API.
- **Amazon SES:** excellent deliverability and pricing at scale. Requires AWS account, IAM credentials, region selection, and verified sending identity — disproportionate setup overhead for a single-function portfolio. Rejected.

### ADR-005 (CSS Loading: `rel="preload"` Defer for Non-Critical Stylesheets)

Decision: load `animate.css` and Google Fonts Roboto as non-blocking resources using `<link rel="preload" as="style" onload="this.onload=null;this.rel='stylesheet'">` with `<noscript>` fallback (2026-03-02).

Rationale:
1. **`animate.css` is not render-critical.** No element visible in the initial viewport (above the fold) uses an `animate.css` class. Loading it synchronously — as a render-blocking `<link rel="stylesheet">` — penalizes every page load for content that does not affect the first paint. Deferring it eliminates the blocking penalty while preserving the animation behavior for below-fold content.
2. **The `@import` pattern in `font.css` created a sequential blocking chain.** Browser must (1) fetch `font.css` (blocking), (2) parse it, (3) discover `@import`, (4) open a new connection to `fonts.googleapis.com` and fetch Roboto CSS (blocking), (5) discover `@font-face` entries, (6) fetch font files from `fonts.gstatic.com`. Steps 1–4 are fully sequential and block rendering. Moving Roboto to a `rel="preload"` in the HTML `<head>` collapses steps 1–4 into a parallel preload that the browser discovers in the same pass as all other resources, while `<link rel="preconnect">` pre-warms the DNS + TLS to both font origins.
3. **`rel="preload"` is semantically correct.** Unlike the `media="print"` workaround (also common), `rel="preload"` explicitly signals to the browser that this resource will be needed soon and should be fetched at high priority — just not applied synchronously. The browser can optimize its fetch scheduling accordingly.
4. **`<noscript>` fallback.** Users with JavaScript disabled receive the stylesheets via the synchronous fallback link, preserving full visual fidelity.
5. **`this.onload=null` guard.** Prevents some browsers from firing the `onload` handler multiple times when `this.rel` is reassigned, which could cause a second application cycle.

Alternatives considered:
- **`media="print"` trick:** `<link rel="stylesheet" media="print" onload="this.media='all'">`. Functionally equivalent and widely supported. `rel="preload"` is semantically cleaner and provides stronger preload priority hints to the browser's resource scheduler. Either approach is valid; `rel="preload"` was chosen for semantic precision.
- **CSS bundler + PurgeCSS:** would eliminate unused CSS from both `animate.css` and `style.css`, reducing their size dramatically. Higher impact but also higher complexity and regression risk (PurgeCSS needs careful safelist configuration for dynamically-applied classes). Deferred to Improvement #7.
- **Self-hosting Google Fonts (Roboto):** eliminates the external origin dependency and the `fonts.googleapis.com` request entirely. `font.css` already self-hosts Josefin Sans and Poppins via `@font-face`. Roboto could be self-hosted in the same file. Deferred — the font files would need to be downloaded, versioned, and kept updated. The preload approach achieves most of the performance benefit with zero additional asset management overhead.

## Validation Standard (Non-Negotiable)

Every relevant sprint must finish with:

1. `npm run build:pages`
2. `npm run test:quality`
3. `npm run test:e2e`

If visual changes are intentional:

1. `npm run test:e2e -- tests/e2e/visual.spec.js --update-snapshots`
2. `npm run test:e2e`

## Superseded Documents

The following point-in-time notes are superseded by this changelog and runbook:

1. `ACCESSIBILITY-HARDENING-PHASE-4-2026-02-25.md`
2. `AUDIT-2026-02-25.md`
3. `COMPONENTIZATION-PHASE-1-2026-02-25.md`
4. `COMPONENTIZATION-PHASE-2-2026-02-25.md`
5. `COMPONENTIZATION-PHASE-3-2026-02-25.md`
6. `COMPONENTIZATION-PHASE-4-2026-02-25.md`
7. `COMPONENTIZATION-PHASE-6-2026-02-26.md`
8. `COMPONENTIZATION-PHASE-7-2026-02-26.md`
9. `COMPONENTIZATION-PHASE-8-2026-02-26.md`
10. `CORE-CAPABILITIES-SECTION-REFRAME-2026-02-26.md`
11. `EDUCATION-SECTION-REFRAME-2026-02-26.md`
12. `EXPERIENCE-CARD-AGLAYA-REFRAME-2026-02-26.md`
13. `EXPERIENCE-CARD-ELM-ST-FOUNDER-NARRATIVE-ARCHITECT-ADDITION-2026-02-26.md`
14. `EXPERIENCE-CARD-LFI-DIRECTOR-REFRAME-2026-02-26.md`
15. `EXPERIENCE-CARD-LFI-FRONTEND-REFRAME-2026-02-26.md`
16. `EXPERIENCE-CARD-LFI-MARKETING-COORDINATOR-REFRAME-2026-02-26.md`
17. `EXPERIENCE-CARD-NIC-EC-MCM-DATE-BLOCK-STYLING-FIX-2026-02-26.md`
18. `EXPERIENCE-CARD-NIC-EC-MKT-COMMS-DIRECTOR-REFRAME-2026-02-26.md`
19. `EXPERIENCE-CARD-NIC-EC-MKT-COMMS-MANAGER-REFRAME-2026-02-26.md`
20. `EXPERIENCE-INTRO-EXPAND-FIX-ELM-ST-2026-02-26.md`
21. `EXPERIENCE-INTRO-EXPAND-FIX-ROLLOUT-2026-02-26.md`
22. `INCIDENT-LAYOUT-HERO-NESTING-2026-02-26.md`
23. `PERFORMANCE-HARDENING-PHASE-5-2026-02-25.md`
24. `PLAYWRIGHT-OPERABILITY-HARDENING-2026-02-26.md`
25. `PORTFOLIO-LEGACY-BRAND-REMOVAL-2026-02-26.md`
26. `PROJECTS-SECTION-STATIC-GRID-SIMPLIFICATION-2026-02-26.md`
27. `SERVICES-GROUP-RESTRUCTURE-ARCHITECTURE-PHASES-2026-02-26.md`
28. `SERVICES-TRANSLATION-SYNC-2026-02-26.md`
29. `VISUAL-BASELINE-SECTIONS-2026-02-26.md`
30. `adr/ADR-001-componentization-strategy.md`
31. `adr/ADR-002-design-tokens-and-css-governance.md`
